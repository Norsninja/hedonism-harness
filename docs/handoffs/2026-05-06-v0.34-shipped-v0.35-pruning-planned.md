# Handoff: v0.34 lineage observability shipped; v0.35 founder-survival-timing planned

**Date:** 2026-05-06
**Branch:** `claude/v0.35-lineage-pruning-founder-survival`
**Base commit (main tip at branch creation):** `e9c7735`
**Status:** v0.34 merged to main this session (PR #29). Aggregate-optimum audit phase closed by v0.33; lineage observability MVP closed by v0.34 (H7 mostly-concentrated fired at h=8). v0.35 scope locked by user; **do NOT autostart implementation** — write a pre-reg first per the per-PR cadence.

## Where we left off

PR #29 squash-merged (`e9c7735`). v0.34 fired **H7 mostly-concentrated** at h=8 of the v0.33 pooled corpus: 22/24 runs single-lineage majority, 8/24 single-lineage monopoly, mean `top_lineage_b50_share` = 0.759. Secondary observation, not pre-committed: dominance rises monotonically with hazard (mean shares 0.635 → 0.728 → 0.759 → 0.785 across h ∈ {0, 4, 8, 12}). User flagged this as the **causal fork** v0.35 should resolve: "does higher hazard increase late-window concentration by pruning founder lines earlier, or by allowing one line to out-reproduce others after all lines survive?" Stay post-hoc one more step.

## What this session shipped

- v0.33 PR #28 squash-merged (`25b2702`): hazard-axis third-stream calibration; H6_pool WEAK fired with locked phrase "Directionally persistent, not mechanistically robust." Three-of-three streams agreed directionally (Δ_low_pool=+9, Δ_high_pool=+9, n_strict_favoring_pool=2/24, n_hazard_insensitive_pool=14/24). Different outcome from v0.31 weight-axis sister.
- v0.34 PR #29 squash-merged (`e9c7735`): post-hoc lineage observability reducer over existing per-run sidecars (`agent_lifetimes.csv`, `config.json`, `manifest.json`); zero `src/` changes; zero events.jsonl parsing. Two cross-check anchors green (H5 `total_b50` byte-identical to v0.33 `B_pool(h)`; H5b 5 founders × 96 runs).
- Test suite **828 → 856 → 883** (+28 v0.33 + 27 v0.34). Ruff / format / smoke green throughout.

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.33.md]] — pre-reg + Results + Conclusion; H6_pool WEAK fires; locked phrase reused verbatim from v0.31.
- [[docs/experiments/fear_hunger_v0.34.md]] — pre-reg + Results + Conclusion; H7 mostly-concentrated fires; secondary monotone-with-hazard observation.
- [[scripts/v0.33_sweep.py]], [[scripts/v0.33_audit.py]] — third-stream sweep + three-stream merge audit.
- [[scripts/lineage_replay.py]] — post-hoc reducer; pure-function pipeline (`assign_founder_lineages`, `compute_generation_depths`, `build_agent_rows`, `aggregate_lineages`, `summarise_run`, `aggregate_pool`, `cross_check_b50_anchors`); `LineageReplayError` halt class.
- [[src/hedonism_harness/experiments/comparison_grid.py]] — `V0_33_TIGHT_H_ARMS` literal slice (element-wise identical to V0_32_TIGHT_H_ARMS by construction).
- [[tests/test_comparison_grid_v0_33.py]], [[tests/test_v0_33_audit.py]], [[tests/test_lineage_replay.py]] — +18 + 10 + 27 tests.

## Decisions locked in

- **v0.33 verdict: H6_pool WEAK.** Locked phrase: "Directionally persistent, not mechanistically robust." Reused verbatim from v0.31.
- **Aggregate-optimum audit phase closed.** v0.21..v0.27 catalog of small-margin optima demoted to "not promoted to mechanism without a new motivating observation." See v0.33 Conclusion.
- **v0.34 verdict: H7 mostly-concentrated.** Lineage lens shows the +9/+9 aggregate at h=8 is per-seed dynasty volatility under aggregate stability, not substrate preference.
- **v0.34 substrate hierarchy:** `agent_lifetimes.csv` is primary; existing per-run `lineages.csv` is cross-check only (its `founder_id` field actually means "first descendant agent_id," not "tick-0 founder"). `events.jsonl` not parsed by v0.34. `trait_fingerprints.csv` deferred to v0.35+.
- **v0.35 scope locked by user (no Mesa, no HedonismPolicy, no heritability yet):** founder survival timing as a targeted follow-up on the v0.34 secondary finding. See user message in this session for verbatim observable list.
- **`SINGLE_LINEAGE_MAJORITY_THRESHOLD = 0.5`** in [[scripts/lineage_replay.py]] is locked per pre-reg.
- **`B_POOL_ANCHORS = {4: 312, 8: 321, 12: 312}`** — v0.33 audit cross-check; future reducer changes that drift these halt v0.34 / v0.35.

## Open questions / blockers

- **Causal fork (the v0.35 question itself):** does higher hazard increase late-window concentration by **pruning founder lines earlier** (extinction-driven mechanism) or by **allowing one line to out-reproduce others after all lines survive** (expansion-driven mechanism)? v0.35 must answer.
- **Whether dominant lineages share systematic traits.** Out of scope for v0.35 (gated to v0.36+ heritability lens). Do not consume `trait_fingerprints.csv` in v0.35.

## Next concrete step

**Write v0.35 pre-reg first.** User's locked scope (verbatim from this session):

```
v0.35 — lineage pruning / founder survival timing
Minimum scope (per run × hazard):
  - founders active at tick {25, 50, 75, 100, 150, 200}
  - tick each founder lineage goes extinct, if extinct
  - b50 contribution by founder line
  - whether the eventual dominant lineage was already dominant before tick 50
  - number of lineages contributing at least one birth after tick 50
Question: pruning vs. dynasty expansion as the mechanism for hazard-driven concentration?
```

Implementation surface: a second post-hoc reducer at [[scripts/lineage_survival_replay.py]] over **the same 96-run corpus** v0.34 used (v0.25 1..8 + v0.32 9..16 + v0.33 17..24, hazard ∈ {0,4,8,12}, influx=1.0). Reuse [[scripts/lineage_replay.py]]'s discovery + parsing helpers (`STREAM_CONFIGS`, `parse_agent_lifetimes`, `parse_config`, `parse_manifest`, `assign_founder_lineages`, `compute_generation_depths`, `LineageReplayError`) — additively, no modification of the v0.34 surface. Output four CSVs under `runs/lineage-v0.35/`: `founder_timeline.csv` (per-run × founder × snapshot tick), `founder_extinction.csv` (per-run × founder), `pre_post_dominance.csv` (per-run; was-the-eventual-winner-already-leading-pre-tick-50), `pruning_summary.csv` (per-hazard mean+median extinction tick + survival rates at each snapshot). Pre-reg should commit a clean H6/H7/H8-style decision rule between **pruning** (mean founders-alive-at-tick-50 declines monotonically with hazard AND eventual-winner-already-dominant rate is low) and **expansion** (pruning rates flat across hazard AND eventual-winner-already-dominant rate rises with hazard).

The reducer **must** also re-anchor against v0.34's `top_lineage_b50` per run as a sanity check that the same b50 attribution is reproducible.

Estimate: ~250 LOC reducer + ~200 LOC tests + ~450 LOC pre-reg/Results doc. Suite 883 → ~905 (+~22).

## Watch-outs

- **Pre-reg before sweep / reducer.** Same discipline that landed v0.31..v0.34 cleanly. Do not skip.
- **Reuse v0.34 helpers additively.** [[scripts/lineage_replay.py]] is in production for the v0.34 outputs; do not modify it. Import via `importlib.util` per the established hyphen-name pattern.
- **Re-anchor against v0.34.** v0.35 should compute `top_lineage_b50` per run and assert it equals v0.34's run_summary.csv values byte-identical for each (source_version, arm_label, seed). Drift halts.
- **Snapshot tick semantics.** "Founder active at tick T" = ∃ agent in that lineage with `birth_tick_normalized ≤ T < (death_tick or n_ticks)`. Pre-commit this in the pre-reg before any code.
- **Eventual-winner-pre-50 needs a tie-break rule.** Mirror v0.34's "lowest lineage_id wins ties" tie-break. Pre-commit.
- **`top_lineage_b50_share = NaN` runs** (none in the 96-run corpus, but defensively) — exclude from the eventual-winner-pre-50 calculation.
- **Do not bundle heritability.** v0.36+ territory.
- **Do not modify any prior `scripts/v0.NN_*.py`.** v0.34 set the precedent of zero-touch on prior scripts; keep that.
- **`runs/lineage-v0.34/` is gitignored** under `runs/`. v0.35 outputs under `runs/lineage-v0.35/` will be too.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   883 passed
uv run python scripts/core_smoke_test.py  ok
uv run python scripts/lineage_replay.py   96 runs reduced; H7 fires at h=8 (re-runnable)
```
