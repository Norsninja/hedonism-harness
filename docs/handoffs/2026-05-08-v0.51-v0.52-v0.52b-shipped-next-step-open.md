# Handoff: v0.51 + v0.52 + v0.52b shipped; next-step open

**Date:** 2026-05-08
**Branch:** `claude/review-hedonism-harness-Ryyx0` (currently aligned with `origin/main` post-squash; future work continues here unless told otherwise)
**Tip commit:** `d2d615f`
**Status:** Channel-decoupling arc complete on V0_25 corpus. Three slices merged this session (v0.51 #49, v0.52 #50, v0.52b #51). Next-step candidates open; user has not locked one. Pre-data design discussion required for whichever slice is picked.

## Where we left off

Three slices shipped end-to-end this session, all squash-merged to main: v0.51 (`FOUNDER_CLAMP_REPRODUCED`), v0.52 (`INTERVENTION_OPPOSITE_SIGN_HALT` — B PRESENT confirms metabolic-cost not necessary; C uniform-max-radius regime-shift halt-loud), v0.52b (`INFORMATION_RADIUS_FOLLOWS_ASSIGNMENT` — between-lineage shuffle preserves the bridge). v0.52 added `BodyConfig.effective_sensor_radius_override` (per-model) + sensors.py resolver. v0.52b added `Traits.effective_sensor_radius_override` (per-agent), filtered out of `TRAIT_NAMES`, and widened `mutate_traits` to use `dataclasses.replace(parent, **values)` for lineage-coherent inheritance. Pytest at handoff time: 1696 passed, 7 skipped. Smoke ok. No `src/` work in flight. No uncommitted changes. Branch was force-pushed after squash to align with `origin/main`.

## What this session shipped

- v0.51 — `FOUNDER_CLAMP_REPRODUCED` (PR #49, squash `c79fd55`); founder + lineage-wide clamp via AgentBorn listener; descendant-drift channel empirically zero on V0_25.
- v0.52 — `INTERVENTION_OPPOSITE_SIGN_HALT` (PR #50, squash `0f7344d`); B confirms cost dispensable, C uniform-max-radius produces wrong-sign Label B; first src/ change in v0.46+ stack.
- v0.52b — `INFORMATION_RADIUS_FOLLOWS_ASSIGNMENT` (PR #51, squash `d2d615f`); per-founder shuffle of `Traits.effective_sensor_radius_override` over preserved global distribution; descendant inheritance via `mutate_traits` widening verified clean (1667/1667).

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.51.md]] — pre-reg + Results; headline caveat about descendant-drift empirically zero.
- [[docs/experiments/fear_hunger_v0.52.md]] — pre-reg + Results; user discipline pass on C interpretation (regime-shift framing, CAN/CANNOT lists).
- [[docs/experiments/fear_hunger_v0.52b.md]] — pre-reg + Results; lineage-coherent inheritance via Option α verified.
- [[scripts/v0_51_descendant_drift_audit.py]] — v0.51 reducer; AgentBorn listener with `weak=False` + `disconnect_callbacks` + `finally`-block disconnect.
- [[scripts/v0_52_information_vs_cost_audit.py]] — v0.52 reducer.
- [[scripts/v0_52b_information_radius_shuffle_audit.py]] — v0.52b reducer; per-founder shuffle with rotate-by-one fallback.
- [[src/hedonism_harness/core/config.py]] — v0.52 added `BodyConfig.effective_sensor_radius_override: int | None = None`.
- [[src/hedonism_harness/core/sensors.py]] — v0.52 + v0.52b resolver pattern at `observe()` (line ~123) and `_read_memory_directional` ValenceMemory branch (line ~196). Per-agent (body.traits) takes precedence over per-model (body_config); `apply_metabolism` and DirectionalMemory branch unchanged.
- [[src/hedonism_harness/core/traits.py]] — v0.52b added `Traits.effective_sensor_radius_override`; filtered from `TRAIT_NAMES`; `mutate_traits` uses `dataclasses.replace(parent, **values)`.

## Decisions locked in

- **Discipline framing on C halts (user-locked v0.52 review)**: never claim "information radius IS load-bearing" or "IS NOT load-bearing" from a halt-loud arm. Always frame as "intervention-incompatible regime shift". CAN-claim / CANNOT-claim lists in v0.52 Results are the canonical pattern.
- **Option α (lineage-coherent inheritance via mutate_traits widening)** chosen over AgentBorn-listener propagation. Inheritance belongs in the trait-mutation pathway, not in an experiment-side birth listener (user-locked v0.52b).
- **`Traits.effective_sensor_radius_override` is on Traits but NOT in TRAIT_NAMES**: filtered out of `random_traits` / `mutate_traits` / `validate_traits` / archetype-builder / metrics collectors. It is data layout, not a biologically-mutable trait.
- **Re-anchor strategy across v0.52 / v0.52b**: Tier-1 only (A_null vs v0.48 six published cells). No Tier-2. Validity anchored by Tier-1 + four implementation-equivalence tests.
- **Resolver priority order** (locked in sensors.py docstring + tests): per-agent (`body.traits.effective_sensor_radius_override`) → per-model (`body_config.effective_sensor_radius_override`) → trait `int(body.traits.sensor_radius)`.

## Open questions / blockers

- User has not locked which slice is next. Multiple candidates open: v0.53 cross-layout (widened_gradient / food_ladder), v0.54 joint ablation (zero-cost AND shuffle), v0.52b uniform-4 deferred secondary (only worth running if a specific question demands it), fresh-stream calibration (longer-horizon).
- No external state blockers. No pending PRs.

## Next concrete step

**`/resume` first, then await user direction on the next slice.** When the user names one, follow the established pre-reg-then-implementation-then-Results pattern: draft pre-reg per the v0.49–v0.52b template (header / pre-implementation correction / conservation framing / corpus / arms / labels / observables / sub-verdicts / rollup / re-anchor / outputs / implementation plan / 16 tests / watch-outs / Files / Results placeholder), commit pre-reg, ask for thumbs-up on rollup naming + open design questions, then implement (src/ change if needed → reducer → 16 tests → CI gate → reducer run → Results section → commit + push → PR → merge after user confirms CI green). For v0.53 the natural arms are A_null + B_widened_gradient + C_food_ladder; for v0.54 they are A_null + B_joint_ablation. v0.52b uniform-4 is a 2-arm primary-only follow-up if activated. Cut work onto `claude/review-hedonism-harness-Ryyx0` (the harness-locked branch — DO NOT cut a new branch unless user authorizes).

## Watch-outs

- **Branch is `claude/review-hedonism-harness-Ryyx0`**, not main. Per harness instructions, all dev + final pushes go here. Force-push with `--force-with-lease` is the canonical post-squash-merge cleanup pattern (used twice this session).
- **Resolver priority is per-agent before per-model**: any future arm using `BodyConfig.effective_sensor_radius_override` will be overridden on per-agent bodies whose `traits.effective_sensor_radius_override` is set. Test #6 in v0.52b enforces this.
- **`apply_metabolism` is NOT a resolver consumer**: it reads `body.traits.sensor_radius` directly (the trait, not the override). v0.52 test #7 + v0.52b test #9 enforce. Future src/ changes must NOT route the override through metabolism without an explicit pre-reg lock.
- **DirectionalMemory branch is intentionally unaffected** by both overrides (v0.52 test #8 + precedent). Modern A_null corpus uses `cell_exact` / `ValenceMemory`; any new memory-path arm must verify this branch coverage.
- **TRAIT_NAMES filter for `effective_sensor_radius_override`** must be preserved if Traits gains more experimental fields in the future. Current pattern: `tuple(f.name for f in fields(Traits) if f.name != "effective_sensor_radius_override")`. If a third experimental field is added, generalize to a frozenset of excluded fields.
- **`mutate_traits` now uses `dataclasses.replace(parent, **values)`** instead of `Traits(**values)`. This preserves any `parent` field not in `values` — including the v0.52b override AND any future Traits field added without TRAIT_NAMES inclusion. Determinism verified by 1696 tests passing without modification.
- **Locked phrases fire verbatim**. v0.51 / v0.52 / v0.52b each have their locked rollup phrases in the script's `LOCKED_PHRASES` dict. No paraphrase.
- **Per-PR cadence**: pre-reg first commit (record contract), implementation+Results second commit. v0.52 / v0.52b followed this pattern.
- **CI gate before push**: ruff + format + pytest + smoke. The smoke test (`scripts/core_smoke_test.py`) is the determinism north star — verify it passes after any src/ change.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok (222 files already formatted)
uv run pytest                   1696 passed, 7 skipped
uv run python scripts/core_smoke_test.py   ok (default behavior preserved under modified sensors.py + traits.py)
```
