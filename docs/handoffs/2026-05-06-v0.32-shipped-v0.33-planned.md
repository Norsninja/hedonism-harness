# Handoff: v0.32 shipped; v0.33 hazard-axis third-stream calibration planned (post-discussion)

**Date:** 2026-05-06
**Branch:** `claude/v0.33-hazard-axis-third-stream-calibration`
**Base commit (main tip at branch creation):** `ab7989e`
**Status:** v0.32 merged to main this session. v0.33 is the canonical next step per the v0.32 pre-reg's H6 decision rule, but **the user explicitly wants to discuss the post-calibration arc before executing v0.33**. Do NOT autostart implementation.

## Where we left off

PR #27 squash-merged (`ab7989e`). v0.32 fired **H6 WEAK** on the v0.25 tight `h*=8` interior-hazard claim against fresh seeds 9..16 (B=91/91/**96**/94 across h ∈ {0,4,8,12}; classifier slice {h=4, h=8, h=12} → Δ_low=+5, Δ_high=+2; n_strict_favoring=0/8). H1c semantic determinism anchor passed (B(h=8, seeds 9..16)=96 matches v0.30 stream 2 explicit-w=1.0). The v0.30→v0.31 weight-axis trajectory now mirrors on the hazard axis. Quarantined secondary finding: v0.25's "small hazard helps tight" (h=0 vs h=4 = +13 on 1..8 → +0 on 9..16) does NOT reproduce; documented as separate independent claim.

User's session-end signal: *"in the next session we will complete .33 after we can audit the next best course together."* Read this as: deliberate before executing — the post-calibration arc deserves a conversation, not a default.

## What this session shipped

- v0.32 PR #27 squash-merged (`ab7989e`): `V0_32_TIGHT_H_ARMS` literal slice of `V0_25_ARMS` at influx=1.0 (4 arms; w=None preserves true v0.25 substrate); `evaluate_audit` additive `candidate_label` refactor (default `"w=0.75"` preserves v0.30/v0.31 byte-identity; v0.32 passes `"h=8"`); `scripts/v0.32_sweep.py` (32 runs in 9.2s); `scripts/v0.32_audit.py` (3-step: H1c semantic anchor, classifier slice verdict, 4-arm descriptive report including `n_hazard_insensitive`); `tests/test_comparison_grid_v0_32.py` (15) + `tests/test_v0_32_audit.py` (8).
- Pre-reg + Results + locked-Conclusion in [[docs/experiments/fear_hunger_v0.32.md]]; per-seed audit report at `runs/fear-hunger-v0.32-tight_gradient/audit.md` (gitignored).
- Test suite 805 → 828 (+23); ruff/format clean; core smoke ok.
- Locked phrase honored: "v0.32 gives fresh-stream H6 reproduction of the v0.25 tight h*=8 local interior-hazard signal, but the result remains directionally persistent rather than mechanistically robust."

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.32.md]] — pre-reg + Results + Conclusion; H6 WEAK fires; H1c anchor passed; h=0 secondary quarantined.
- [[src/hedonism_harness/experiments/comparison_grid.py]] — `V0_32_TIGHT_H_ARMS = tuple(arm for arm in V0_25_ARMS if arm.label.endswith("-influx-1.0"))`.
- [[scripts/v0.30_audit.py]] — `evaluate_audit(..., *, candidate_label="w=0.75")` additive default-preserving refactor; H7/H8 labels parameterised.
- [[scripts/v0.32_sweep.py]] — 32-run sweep on V_32 arms × seeds 9..16.
- [[scripts/v0.32_audit.py]] — 3-step audit driver; `weight_labels` overridden to map slot keys onto hazard arm labels.
- [[tests/test_comparison_grid_v0_32.py]], [[tests/test_v0_32_audit.py]] — +15 + 8 tests.

## Decisions locked in

- **v0.32 verdict: H6 WEAK.** Single-stream fresh reproduction; not yet confirmed, not yet demoted. Calibration phase continues.
- **`candidate_label` parameter is the canonical v0.32+ pattern** for label-correctness when the classifier is reused on non-weight axes. Default preserves all prior reports byte-identical.
- **H1c semantic determinism anchor is durable.** V0_25_ARMS substrate (`hazard_avoidance_weight=None`) is observationally equal to V0_27+ explicit-w=1.0 at the audit cell. Future hazard-axis audits can rely on this.
- **"Small hazard helps tight" is quarantined as an independent claim.** Must NOT be bundled with the `h*=8` local-interior audit. Needs its own fresh-stream replication if revisited.
- **v0.33 implementation surface is fully documented** in the v0.32 pre-reg's H6 decision rule — copy-paste of the v0.31 audit pattern with hazard slot mapping.

## Open questions / blockers

- **The post-calibration arc.** After v0.33 closes the hazard axis, the methodological calibration phase ends. User flagged this needs deliberate discussion next session BEFORE executing v0.33. Candidate directions surfaced in this session's "minimap":
  - **Catalog of other ≤6-birth optima from v0.21..v0.27** (deferred since v0.30 handoff).
  - **HedonismPolicy comparisons** (currently quarantined per v0.2 spec; would need a methodological gate before lifting).
  - **Mesa wrapper migration** (handoff template's "scientific core complete, awaiting Mesa go-ahead" string suggests a major architectural step waiting).
  - **Per-agent / lifespan / heritability inspection** (consistently out-of-scope but accumulating motivation).
  - **The quarantined "small hazard helps tight" claim** as a short follow-up audit.
- **Should v0.33 even run first?** The user's framing suggests they want to weigh "default to v0.33 then move on" vs. "step back and re-prioritise" before committing to the next sweep.
- **Remote branches.** `claude/v0.32-...` was pruned by the user. Earlier `claude/v0.26..0.31-...` may still be present remotely; sandbox proxy cannot delete them.

## Next concrete step

**FIRST: discuss the post-calibration arc with the user.** Surface the candidate directions (catalog audits / HedonismPolicy / Mesa / per-agent inspection / `h=0` quarantine follow-up) and let the user pick the order. Do NOT default to executing v0.33 without that conversation.

**THEN, if the user greenlights v0.33:** the implementation surface is fully specified by the v0.32 pre-reg's H6 decision rule and mirrors v0.31 verbatim except for hazard slot mapping. Single target: tight_gradient × influx=1.0 × hazard ∈ {0, 4, 8, 12} × seeds **17..24** (32 runs; ~10s; mirrors v0.32 sweep). The v0.33 pre-reg should:

- Frame narrowly: *"Does the H6 WEAK directional signal on the hazard axis compound across three independent 8-seed streams, or does it collapse?"* — NOT mechanism promotion.
- Reuse `evaluate_audit(b50_at, seeds, candidate_label="h=8")` for both single-stream and pooled invocations.
- Pre-commit a 24-seed pooled rule using the same linearly-scaled thresholds v0.31 used: `AuditThresholds(15, 15, 3, 15)`. Locked phrase for H6_pool: **"Directionally persistent, not mechanistically robust."** (Reusable verbatim from v0.31.)
- Define `V0_33_TIGHT_H_ARMS` as a fresh literal slice of V0_25_ARMS at influx=1.0 (will be element-wise identical to V0_32_TIGHT_H_ARMS by construction).
- 3-stream load: stream 1 = v0.25 source artifacts (`runs/fear-hunger-v0.25-tight_gradient/arms/transfer-1500-hzd{0,4,8,12}-influx-1.0/seed-{1..8}/`), stream 2 = v0.32 (seeds 9..16), stream 3 = v0.33 (seeds 17..24). Verify v0.25 artifacts on disk before drafting (a la v0.31's option-(a) check).
- **Carry forward `n_hazard_insensitive_pool ≥ 18/24`** as candidate primary finding (mirrors v0.31's `n_weight_insensitive_pool` at the same threshold).

Driver scripts: `scripts/v0.33_sweep.py` (single sweep on seeds 17..24) + `scripts/v0.33_audit.py` (single-stream verdict + cross-stream comparison table + pooled 24-seed verdict).

**Do not** introduce a finer hazard grid (gated behind H5_pool ROBUST).
**Do not** add cross-influx audit (gated behind H5_pool ROBUST).
**Do not** sweep seeds 25..32. One additional stream per PR.
**Do not** bundle the `h=0` "small hazard helps tight" quarantined claim into v0.33.

## Watch-outs

- **Post-calibration discussion is the gate.** The user explicitly asked for this. Default-execution behaviour is wrong here; reflect on the minimap first.
- **Pre-reg the pooled 24-seed rule BEFORE seeing the data.** Same discipline v0.31 used.
- **v0.25 artifact loading.** v0.33 audit needs `runs/fear-hunger-v0.25-tight_gradient/` events.jsonl on disk for stream 1. Verify before implementation; if missing, decide between (a) on-disk if available or (b) staged regen — same pattern v0.31 followed for v0.27.
- **Verdict reachability.** v0.30 source data was Δ_low=+5 / Δ_high=+2 (H6); v0.32 fresh stream Δ_low=+5 / Δ_high=+2 (H6 — identical pattern). Pooled 1..16 will give Δ_low=+8 / Δ_high=+4 — H6_pool already fires under the proposed rule (Δ ≥ 3). Stream 3 only tips to H7_pool if it strongly inverts. The H5_pool ROBUST bar (Δ ≥ 15) is essentially out of reach. Genuinely live verdict space is H6_pool ↔ H7_pool, same as v0.31 was for the weight axis.
- **Per-seed pattern likely to repeat.** v0.30 had n_weight_insensitive=6/8 → pool 17/24. v0.32 had n_hazard_insensitive=4/8. If v0.33 stream 3 has ≥6/8, pooled may land near the 18/24 candidate-finding bar.
- **Locked H6_pool phrase reuse.** "Directionally persistent, not mechanistically robust." — same wording, same discipline. Do not paraphrase.
- **Test suite at 828.** Pure-additive v0.33 should bring it to ~850.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   828 passed
uv run python scripts/core_smoke_test.py  ok
```
