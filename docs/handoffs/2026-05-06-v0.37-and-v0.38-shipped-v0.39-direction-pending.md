# Handoff: v0.37 + v0.38 shipped same session; v0.39 direction user-locked-pending

**Date:** 2026-05-06
**Branch:** `main` (no active feature branch; cut v0.39 from `ecc38e2`)
**Tip commit:** `ecc38e2`
**Status:** Two slices merged this session (v0.37 #33, v0.38 #34). Five-slice timing-locus arc closed correlationally on the v0.34 corpus. Next slice direction requires user lock between (a) fresh-stream calibration on H5 — discipline-canonical — and (b) per-lineage post-50 mortality decomposition — most informative new mechanism axis. **Do NOT autostart implementation** — bring v0.39 framing to user, get sign-off, then inventory phase.

## Where we left off

v0.38 fired **H5 LEADER-ADVANTAGE-AMPLIFIED**: `mean_leader_advantage` rises strictly monotonically across hazards (3.948 → 7.375 → 8.531 → 8.990; spread +5.042 ≥ locked 1.5 bar). v0.37's stabler-early-leadership signal cashes out as measurable hazard-amplified post-50 reproductive advantage. The compound v0.34..v0.38 arc paints a coherent correlational story: hazard amplifies the post-50 reproductive payoff of holding the early lead AND amplifies the probability that the early leader becomes the eventual winner. Founder traits (v0.36) set who can compete but are hazard-flat. **No mechanism declaration** — promotion requires fresh-stream calibration analogous to v0.30..v0.33.

User signaled v0.39 direction is open; pre-handoff discussion proposed four candidates (a/b/c/d in [[docs/experiments/fear_hunger_v0.38.md]] Conclusion). User direction pending.

## What this session shipped

- v0.37 PR #33 squash-merged (`b5538d5`): dominance timing decomposition reducer + 39 tests; **H6 STABLER-EARLY-LEADERSHIP**; locked four-way verdict (H5/H6/H7/H8).
- v0.38 PR #34 squash-merged (`ecc38e2`): leader post-50 reproductive advantage reducer + 34 tests; **H5 LEADER-ADVANTAGE-AMPLIFIED**; locked three-way verdict (H5/H6/H7).
- Test suite **883 → 924 → 972 → 1005 → 1039** across the five-slice arc (+34 v0.38 in this session, on top of +39 v0.37). Ruff / format / smoke green throughout. Six skips remain (v0.23/v0.27 corpus-dependent; non-regression).
- Local `runs/` corpus regenerated this session: v0.25 + v0.32 + v0.33 sweeps (~85s total wall) + v0.34 + v0.35 + v0.37 + v0.38 reducers. **Local-only; gitignored.** Next session must re-run sweeps + reducers if it needs the corpus on disk (artifact manifest still pending — see Watch-outs).

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.37.md]] — v0.37 pre-reg + Results; H6 STABLER-EARLY-LEADERSHIP fires; O1 fails-on-monotone (h=4→h=8 reverses by 2.584 ticks); O2 passes at the bar exactly (spread 0.250 = 0.25 threshold); O3 supporting passes.
- [[docs/experiments/fear_hunger_v0.38.md]] — v0.38 pre-reg + Results; H5 LEADER-ADVANTAGE-AMPLIFIED fires; pooled spread +5.042; decomposition shows wad_T plateau (8.18→11.08 saturating by h=8) + bucket-shift contribution; counterfactual holding wad-rate fixed yields +1.70 within-bucket spread (still ≥ 1.5).
- [[scripts/lock_in_timing_replay.py]] — v0.37 reducer (~530 LOC); imports v0.34 + v0.35 helpers via importlib.util.
- [[scripts/leader_advantage_replay.py]] — v0.38 reducer (~485 LOC); imports v0.34 + v0.35 helpers; double-anchors against v0.34 top_lineage_id AND v0.35 (leader_lineage_id_at_tick_50, eventual_top_lineage_id) — three-column anchor.
- [[tests/test_lock_in_timing_replay.py]] — 39 tests.
- [[tests/test_leader_advantage_replay.py]] — 34 tests.

## Decisions locked in

- **v0.37 verdict: H6 STABLER-EARLY-LEADERSHIP.** Locked rule preferred H6 over a spread-only-but-non-monotonic H7-equivalent because monotonicity is the mechanistic backbone of "tightens with hazard." O1's 27.875-tick spread does NOT fire H5 because of the h=4→h=8 reversal; this is the rule doing its job.
- **v0.38 verdict: H5 LEADER-ADVANTAGE-AMPLIFIED.** No minimum-absolute-O1 bar locked (spread + monotonicity only); the verdict does NOT claim leader_advantage is large in absolute terms, only that it strengthens with hazard. (User-tightened H6/H7 phrasing in pre-reg explicitly avoids "exists" language.)
- **v0.38 internal consistency check:** `n_wad_true` per hazard byte-matches v0.35 wad_rate × 24 (11/17/18/19 of 24 = 0.458/0.708/0.750/0.792). Sanity confirmation that v0.38 reduces the same wad assignment as v0.35.
- **Conservation:** zero `src/` edits across v0.34..v0.38; zero edits to prior `scripts/v0.NN_*.py`; [[scripts/lineage_replay.py]] / [[scripts/lineage_survival_replay.py]] in production from v0.35 / v0.37 / v0.38; [[scripts/trait_replay.py]] in production from v0.36; [[scripts/lock_in_timing_replay.py]] in production from v0.37.
- **Re-anchor stack now triple-deep with multi-column at v0.35:** v0.38 anchors against v0.34 `run_summary.csv:top_lineage_id` AND v0.35 `pre_post_dominance.csv:(leader_lineage_id_at_tick_50, eventual_top_lineage_id)` — both v0.35 columns checked. v0.39+ should continue this discipline.
- **Cautious framing locked across the arc:** five-slice correlational story on the v0.34 corpus; promotion to mechanism requires fresh-stream calibration analogous to v0.30..v0.33 — explicitly out of scope for v0.34..v0.38.

## Open questions / blockers

- **v0.39 direction is user-locked-pending.** Pre-handoff discussion proposed four candidates:
  - **(a) Fresh-stream calibration on H5** — discipline-canonical; replay v0.38 H5 verdict on a fresh seed range to test cross-stream compounding. Only path to mechanism promotion. Conservative; doesn't open new axes.
  - **(b) Per-lineage post-50 mortality / extinction-tick analysis** — disentangles "leader reproduces more" vs "non-leaders die more" as the source of `leader_advantage`. Could explain v0.38's wad_T plateau (saturated by mortality) vs reproduction-driven reading.
  - **(c) Decompose `b50` into multiple post-50 windows** ({50–100, 100–150, 150–200}) — does the leader's advantage build early and plateau, or accumulate? Tests temporal structure of the wad_T plateau.
  - **(d) Descendant-trait drift** — original v0.37-deferred branch; less sharp than (a/b/c) given v0.36's hazard-flat founder-trait finding.

  I leaned (a) or (b); user signaled "let's discuss" but session ended before lock. **Bring all four to user, get verdict-driver lock before pre-reg drafting.**

- **Wad_T plateau (8.18 → 11.08, saturating by h=8) is mechanistically suggestive** but not pre-committed — described in v0.38 Results as secondary observation. Possibly related to v0.34's `B_POOL_ANCHORS` (h=8: 321; h=12: 312 — total post-50 births decline slightly at h=12 vs h=8, suggesting a structural ceiling). Could be a v0.39 angle if user picks (b) or (c).
- **Winner-overtake in wad=False is hazard-flat / slightly declining** (4.00 → 4.14 → 3.67 → 3.40). When the late-emerging winner overtakes from non-leadership, magnitude is hazard-independent or modestly suppressed. Hazard suppresses comeback magnitude, not amplifies it.

## Next concrete step

**Open v0.39 framing discussion with user.** Bring the four candidates (a/b/c/d above) with their tradeoffs. Get user lock on which observable family drives v0.39. Then **cut `claude/v0.39-<slug>` branch from `ecc38e2`** with the locked-in slug. Then inventory phase (no aggregation, no thresholds — same discipline as v0.37 / v0.38: 3 representative runs, observable computability + sentinel sanity check, bring back to user). Then pre-reg drafting with locked thresholds. Then implementation.

If user picks **(a) fresh-stream calibration**: the slice will need a fresh sweep at seeds 25..32 (or similar untested range), which adds ~30s wall time per stream. Inventory phase confirms the new sweep produces the same shape. Pre-reg locks the H5 replay verdict against the new corpus. Note: this is the FIRST slice in the v0.34..arc that adds a new sweep (v0.34..v0.38 are all post-hoc reducers).

If user picks **(b) per-lineage post-50 mortality**: pure post-hoc reducer, no new sweep. Observable family: per-lineage `extinction_tick` and post-50 mortality rate, conditional on tick-50 leadership status. v0.35's `compute_extinction_tick` is the entry point; reuse pattern straightforward.

If user picks **(c) windowed b50**: pure post-hoc reducer. Observable family: `b50_50_to_100`, `b50_100_to_150`, `b50_150_to_200` per lineage. Tests temporal structure.

If user picks **(d) descendant drift**: the original v0.37 deferred branch from the previous handoff. Pure post-hoc reducer. v0.36 trait-axis lens, but on descendants instead of founders. Big trait-fishing risk (13 traits × multi-generational); requires the same fishing-prevention discipline as v0.36.

## Watch-outs

- **CLAUDE.md ([[CLAUDE.md]]) is on main and auto-loads.** `/resume` should read it before the latest handoff. Don't re-derive its protocols from session transcripts.
- **Pre-reg before any sweep / reducer.** v0.39 must not skip the inventory-then-lock-then-implement cadence. v0.37 + v0.38 reinforced the discipline; carry it forward verbatim.
- **Reuse v0.34/v0.35 helpers additively.** [[scripts/lineage_replay.py]] and [[scripts/lineage_survival_replay.py]] are in production. [[scripts/trait_replay.py]] (v0.36), [[scripts/lock_in_timing_replay.py]] (v0.37), [[scripts/leader_advantage_replay.py]] (v0.38) are also in production but structurally orthogonal to most v0.39 candidates — likely NOT imported. Confirm before pre-reg.
- **Re-anchor against v0.34 + v0.35 (multi-column).** v0.38 set the precedent of checking both v0.35 leader and winner columns; continue this discipline. Drift halts via `LineageReplayError`.
- **Local `runs/` is gitignored and was regenerated this session.** Next session must re-run `v0.25_sweep` + `v0.32_sweep` + `v0.33_sweep` + `lineage_replay` + `lineage_survival_replay` (and possibly `lock_in_timing_replay` / `leader_advantage_replay` if needed for re-anchor). Wall time: v0.25 ~60s, v0.32 + v0.33 ~13s each in parallel. Reducers <5s each.
- **Artifact manifest deferred TWICE now** (v0.37 handoff also flagged it). User suggested it in v0.37 inventory phase: a small non-git doc at `docs/artifacts/v0.34_v0.35_local_corpus_manifest.md` listing regen commands + expected anchor values. Still not written. **Consider writing it before any v0.39 inventory** to save the next-session-after's regeneration confusion.
- **wad_T plateau is suggestive of structural ceiling** (h=8 mean_O1_wT 11.097, h=12 mean_O1_wT 11.079 — essentially flat). Possibly food-pool-limited (B_POOL_ANCHORS 321 → 312 across h=8 → h=12). If v0.39 (b) or (c) is chosen, this is the prime candidate observation to mechanistically follow.
- **No ROBUST tier in v0.34..v0.38.** All five slices are post-hoc on a single corpus. v0.39 (a) is the FIRST candidate that could open the ROBUST path. Locked phrases will need an "ON BOTH STREAMS" qualifier if (a) fires.
- **Don't bundle HedonismPolicy or Mesa.** Both remain deferred indefinitely per CLAUDE.md.
- **`runs/lineage-v0.39/` will be gitignored** under `runs/`. Outputs are local-regenerable.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   1039 passed, 6 skipped (v0.23/v0.27 corpus
                                skips; non-regression)
uv run python scripts/core_smoke_test.py            ok
uv run python scripts/lock_in_timing_replay.py      H6 STABLER-EARLY-LEADERSHIP
                                                    (re-runnable)
uv run python scripts/leader_advantage_replay.py    H5 LEADER-ADVANTAGE-AMPLIFIED
                                                    (re-runnable)
```

## Strategic state at handoff (updated through v0.38)

| domain | finding |
|---|---|
| Aggregate optima | mostly fragile (v0.21..v0.27) |
| h=8 directional | persistent, not mechanistically robust (v0.33 H6_pool WEAK) |
| Lineage dominance | dynastic (v0.34 H7) |
| Pre-50 pruning as timing locus | rejected (v0.35 H5) |
| Early-leader continuity | supported (v0.35 H6 ≡ EARLY-LEADER CONTINUITY) |
| Founder-trait predictors | sensor_radius dominant, hazard-flat (v0.36 H6) |
| Eventual-winner-emergence-timing | NOT cleanly explained (v0.37 O1 fails monotone) |
| Early-leadership stability | tightens with hazard (v0.37 H6 + supporting O3) |
| Post-50 reproductive payoff of leadership | rises with hazard (v0.38 H5) |
| Fresh-stream calibration on the lineage axis | **not yet performed** — gating mechanism promotion |

The v0.34..v0.38 arc is correlationally complete on the single corpus. The discipline-canonical next move is fresh-stream calibration; the mechanism-most-informative next move is per-lineage mortality decomposition. **User direction pending.**
