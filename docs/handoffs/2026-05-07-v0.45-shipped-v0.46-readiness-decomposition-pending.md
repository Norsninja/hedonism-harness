# Handoff: v0.45 shipped; v0.46 readiness-decomposition pending

**Date:** 2026-05-07
**Branch:** `main`
**Tip commit:** `75f059b`
**Status:** v0.45 squash-merged. v0.46 direction = decomposition (not intervention) on existing corpus, pending corpus selection + pre-reg.

## Where we left off

v0.45 (`BIRTH_LOCALITY_NOT_NECESSARY`) merged via PR #42 at `75f059b`. Headline: dominance share at h=8 reconstitutes within 0.003 of A even with B's preserved-parent-adjacency at 0.07–0.08 — the offspring-placement layer post-tick-50 is not the necessary mechanism. **Explicit no-claims locked in the merged Results**: spatial mechanisms NOT impossible, substrate-causal arc NOT closed. Live causal candidates: pre-50 spatial sorting, founder-position advantage, trait-linked reproduction readiness, parent-energy / above-threshold-fraction at tick 50, action-order/timing, topology-mediated energy access ticks 0..50. **User's locked v0.46 direction**: decomposition (post-hoc reducer over existing corpus, no `src/` changes, no new sweep), NOT intervention. v0.46 cannot start until corpus is chosen — open question below.

## What this session shipped

- v0.44 pre-reg + impl + sweep + audit + 80 tests + Results — `RESPAWN_FLOW_NOT_NECESSARY` — squash-merged via PR #41.
- v0.44 preflight script (seeds 57..64) committed at `42cb678`.
- v0.45 pre-reg + spatial-clustering preflight (b91b4d3) + impl + sweep + audit + 79 tests + Results — `BIRTH_LOCALITY_NOT_NECESSARY` — squash-merged via PR #42.
- First sim-mechanics modification to `core/reproduction.py` since v0.27: additive keyword-only `birth_redirect_callback` parameter, default-None preserves byte-identity, invalid return raises `ValueError`.
- New RNG stream `v0_45_birth_position_intervention` spawned from `streams.mutation`.

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.44.md]] — pre-reg + Results + locked phrase fired.
- [[docs/experiments/fear_hunger_v0.45.md]] — pre-reg + Results + locked phrase fired + corrected conclusion (no "arc closed", no "spatial mechanisms impossible").
- [[src/hedonism_harness/core/events.py]] — `RespawnScheduleByIntervention`, `BirthRedirectedByIntervention` event classes.
- [[src/hedonism_harness/core/interventions.py]] — v0.44 + v0.45 kinds, helpers, `_is_v045_birth_safe_placeable` predicate (HAZARD/WALL excluded), `make_v045_birth_redirect_callback` factory.
- [[src/hedonism_harness/core/reproduction.py]] — additive keyword-only `birth_redirect_callback` parameter on `process_reproduction()`.
- [[src/hedonism_harness/model.py]] — `tick > 50` gate at the agent reproduction call site.
- [[src/hedonism_harness/experiments/fear_hunger_chamber.py]] — callback construction wired into setup; `model.v045_skipped_redirect_counter` initialised.
- [[scripts/v0_45_preflight.py]] — spatial-clustering preflight (mean cluster purity 0.708 on seeds 65..72; PASS all 4 criteria).

## Decisions locked in

- **v0.46 = decomposition, not intervention.** Tick-50 readiness audit on existing corpus. Targets: living count, total/mean energy, agents above reproduction threshold, age eligibility, valid adjacent reproduction cells, cumulative pre-50 births, food/energy acquired pre-50, trait distributions, local hazard signal — by lineage.
- **No claim that the substrate-causal arc is closed** (see v0.45 Results §"Caveats"). v0.45 specifically rules out the post-50 offspring-placement layer; nothing more.
- **Methodological lesson preserved (from v0.43R).** Feasibility probes / decomposition reads must use `_run_one_arm_seed` or operate on already-written events.jsonl; never hand-rolled `run_chamber`.

## Open questions / blockers

- **Which corpus does v0.46 reduce over?** The canonical lineage corpus is v0.34's (seeds 1..24 × 4 hazards × ~24 runs = 96 runs on disk, gitignored). v0.42–v0.45 sweeps (seeds 41..72) are also on-disk but only A_null arms are useful for decomposition (treatment arms perturb the substrate). Three options: (a) v0.34 96-run corpus only — most data, requires `runs/` regeneration; (b) v0.42–v0.45 A_null arms only (32 runs across seeds 41..72, h ∈ {0, 8}); (c) v0.34 + v0.42–v0.45 A_null union. **No `runs/` artifacts exist on local disk** (gitignored); whichever corpus is chosen, the relevant sweeps must be re-run first.
- **Decomposition vs replay.** events.jsonl has AgentBorn/AgentDied/AteFood/HazardDamageApplied per-event — sufficient to reconstruct living-agent positions, lineage membership, and energy (via per-tick metabolism + Eat events). But reconstructing tick-50 *energy state* requires a full replay path. v0.35's `lineage_survival_replay.py` is the precedent — same pattern. Question: build a fresh `tick50_readiness_audit.py` reducer, or extend v0.35's replay?

## Next concrete step

Cut `claude/v0.46-tick50-readiness-decomposition` from main at `75f059b`. Run `/resume` to load this handoff first. Then surface the two open questions to the user (corpus choice + decomposition-vs-replay path) and draft `docs/experiments/fear_hunger_v0.46.md` after they resolve. v0.46 follows the v0.34/v0.35/v0.36 post-hoc-reducer pattern: NO `src/` changes, NO new sweep, just a new script under `scripts/` + paired test file. Pre-commit any chosen corpus regeneration via `uv run python scripts/v0.NN_sweep.py` (write down which sweeps fired before reducer runs).

## Watch-outs

- **`runs/` is gitignored.** Any reducer requires re-running the relevant sweep(s) first. v0.34 sweep is the canonical lineage corpus; v0.42 A_null + v0.43R A_null + v0.44 A_null + v0.45 A_null are all available (re-run their sweeps, ~17s each).
- **Pre-50 reproduction must remain byte-identical** through v0.46. v0.45 already verified this via H2e regression in [[tests/test_world_intervention_hook_v0_45.py]]. Decomposition reducers do NOT touch `src/`.
- **`process_reproduction()` now has a keyword-only `birth_redirect_callback` parameter.** Default-None preserves v0.21..v0.44 byte-identity; any new caller must pass it as a keyword (positional after the `*` barrier raises `TypeError`).
- **The 4 prior intervention paths** (v0.42 leader-kill, v0.43R density, v0.44 respawn-schedule, v0.45 birth-redirect) all remain dispatchable on main; H2e regression covers all of them.
- **Determinism north star** still passes via `scripts/core_smoke_test.py` ("All three stages passed.").

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   1573 passed, 7 skipped
uv run python scripts/core_smoke_test.py   ok
```
