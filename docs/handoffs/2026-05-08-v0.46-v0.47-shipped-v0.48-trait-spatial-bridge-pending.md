# Handoff: v0.46 + v0.47 shipped; v0.48 trait→space bridge pending

**Date:** 2026-05-08
**Branch:** `main`
**Tip commit:** `db128b1`
**Status:** Two decomposition slices over the modern A_null corpus shipped this session. v0.48 = narrow `sensor_radius → space → readiness` bridge test, locked direction by user, pending pre-reg.

## Where we left off

v0.46 (`READINESS_PREDICTS_DOMINANCE`) merged via PR #43 at `d87dbe2`; v0.47 (`READINESS_TRAITS_PARTIALLY_PREDICTIVE`) merged via PR #44 at `db128b1`. v0.46 established that the eventual top lineage is already differentially advantaged at tick 50 along readiness axes (paired_d=+1.136 momentum, +0.810 readiness fraction; mean_energy borderline at +0.452). v0.47 fingered **`sensor_radius`** as the only firing primary trait under the readiness-fraction label (signed_d=+0.937, large effect; cross-corpus echo of v0.36's b50/winner-label finding). `reproduction_drive` and `metabolic_rate` were null (signed_d −0.144 and +0.178 respectively). User has locked v0.48 direction = narrow trait→space→readiness bridge test on `sensor_radius` only — NOT a kitchen-sink spatial decomposition.

## What this session shipped

- v0.46 pre-reg + reducer + 14 tests + Results — `READINESS_PREDICTS_DOMINANCE` — squash-merged via PR #43 at `d87dbe2`.
- v0.47 pre-reg + reducer + 14 tests + Results — `READINESS_TRAITS_PARTIALLY_PREDICTIVE` — squash-merged via PR #44 at `db128b1`.
- Modern-corpus reducer pattern established: deterministic re-execution under each version's locked V0_25 + A_null arm config, no `runs/` artifacts read; cross-version re-anchor against published `a_share_h8` within 1e-3.
- v0.46 introduced `tick50_above_threshold_fraction` / `_count` and `pre50_reproductive_momentum_count`; v0.47 reused the same observer scaffold and added per-lineage founder-trait capture at `setup_observer` time.

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.46.md]] — pre-reg + Pre-implementation correction + Results (3 corrections logged pre-data: dominance label = argmax(b50_count); v0.43R skipped from re-anchor halt; tick_observer path replaces events.jsonl arithmetic).
- [[docs/experiments/fear_hunger_v0.47.md]] — pre-reg + Pre-implementation correction (v0.36 history + NaN count wording) + Results + scope-narrow + zero-count sensitivity note.
- [[scripts/v0_46_tick50_readiness_audit.py]] — self-contained reducer; uses `setup_observer` + `tick_observer` + AgentBorn signal listener.
- [[scripts/v0_47_founder_trait_readiness_audit.py]] — self-contained reducer; founder-trait capture + dual-label paired_d (fraction primary, count descriptive) + agreement rate.
- [[tests/test_v0_46_tick50_readiness_audit.py]] — 14 locked tests, all pass.
- [[tests/test_v0_47_founder_trait_readiness_audit.py]] — 14 locked tests, all pass.

## Decisions locked in

- **v0.48 direction = `sensor_radius → space → readiness` bridge test.** Narrow, NOT intervention.
- **v0.48 corpus** = same modern A_null 64 runs (v0.42/43R/44/45). Same V0_25 anchor.
- **v0.48 primary trait** = `sensor_radius` only (singled out by v0.47).
- **v0.48 spatial/foraging observables (locked candidate set, narrow):** (1) `pre50_food_events_count`; (2) `pre50_food_energy_acquired`; (3) mean distance to nearest FOOD cell over ticks 0..50 (if reconstructible); (4) mean local food/hazard signal over ticks 0..50 (if already available); (5) founder/lineage centroid distance to food cluster at tick 50 (if reconstructible).
- **v0.48 primary test:** `high_sensor_radius_lineage` (per-run argmax of founder `sensor_radius`) vs. non-high lineages, paired within-run deltas, expected signs pre-committed.
- **v0.48 secondary bridge:** do the same spatial/foraging observables predict `high_tick50_readiness_fraction_lineage` (the v0.47 primary label)?
- **v0.48 verdicts (locked names, no causal overclaim):** `SENSOR_RADIUS_SPATIAL_BRIDGE_PRESENT` / `_PARTIAL` / `_NOT_FOUND` / `SPATIAL_BRIDGE_OPPOSITE_SIGN_HALT`.
- **Causal escalation NOT in v0.48.** v0.49 candidate (only if v0.48 fires): equalize food access, scramble food visibility, or clamp sensor radius — first-class intervention with byte-identity guards.
- **Re-anchor protocol** (cross-version `a_share_h8` against published 0.652/0.878/0.818 within 1e-3) reused unchanged from v0.46/v0.47.

## Open questions / blockers

- **Reconstructibility of observables (3) and (5).** Distance-to-food and distance-to-food-cluster need world geometry + per-tick agent positions. v0.46/v0.47 capture only the tick-50 living snapshot. v0.48 will need either (a) a richer per-tick observer that snapshots positions at every tick 0..50, or (b) post-hoc reconstruction from `events.jsonl` (`AgentMoved` + `AteFood`). User to confirm preferred path.
- **Existing per-tick metrics** for observable (4) — there may already be a `LocalFoodSignal`-equivalent emitter or sensor-layer log; check `core/sensors.py` and `core/events.py` before designing capture.

## Next concrete step

Cut `claude/v0.48-sensor-radius-spatial-bridge` from main at `db128b1`. Run `/resume` to load this handoff first, then surface the two open questions (per-tick capture path; existing local-food-signal availability) before drafting `docs/experiments/fear_hunger_v0.48.md`. Pre-reg structure: corpus = same modern A_null 64 runs; primary observable set = the 5 spatial/foraging items locked above (only the reconstructible subset; flag any dropped); primary label = `argmax(founder_sensor_radius)` per run with min(`lineage_id`) tiebreak; effect-size = sign-aware paired Cohen's d ≥ +0.5 fires / ≤ −0.5 halts (matches v0.46/v0.47); 4 verdicts as locked above. Conservation framing unchanged from v0.46/v0.47: NO `src/` modifications; reducer is self-contained `scripts/v0_48_sensor_radius_spatial_bridge_audit.py` + paired test file.

## Watch-outs

- **Do not let v0.48 expand into a kitchen-sink spatial decomposition.** User explicitly named this risk. Stay with the 5 locked observables; fishing-protection per v0.36 / v0.47 (small motivated subset, expected signs pre-committed).
- **Causal language is forbidden in v0.48.** "Bridge present" is the strongest claim allowed. Mechanism declarations require fresh-stream calibration analogous to v0.30..v0.33; v0.48 is observational.
- **`sensor_radius` is integer.** Coerced to float in paired_d arithmetic per v0.47 pattern; per-lineage `argmax` will hit ties frequently — lock min(`lineage_id`) tiebreak in pre-reg.
- **v0.46 / v0.47 reducer scripts must remain byte-identical to merged forms.** v0.48 copy-locals helpers if needed; does NOT mutate prior reducer or audit scripts.
- **Pre-50 reproduction byte-identity** through v0.48 is preserved by construction: v0.48 makes no `src/` change.
- **Re-anchor reference values still hardcoded** (0.652 / 0.878 / 0.818 for v0.42 / v0.44 / v0.45). v0.43R remains informational-only.
- **All 4 prior intervention paths** (v0.42, v0.43R, v0.44, v0.45) remain dispatchable on main; H2e regression covers all of them.
- **Determinism north star** still passes via [[scripts/core_smoke_test.py]] ("All three stages passed.").

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   1601 passed, 7 skipped
uv run python scripts/core_smoke_test.py   ok
```
