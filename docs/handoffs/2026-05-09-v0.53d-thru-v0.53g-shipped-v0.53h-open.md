# Handoff: substrate-axis stack v0.53d–v0.53g shipped; v0.53h N_TICKS-extension is the locked next slice

**Date:** 2026-05-09
**Branch:** `claude/review-hedonism-harness-Ryyx0` (currently aligned with `origin/main` post-squash; future work continues here unless told otherwise)
**Tip commit:** `ec2add2`
**Status:** Substrate-axis stack on `widened_gradient` × V0_25 closed for the conservative founder-facing budget envelope. Four slices merged this session (v0.53d #55, v0.53e #56, v0.53f #57, v0.53g #58). Central scientific finding: **survival-extension and food-reachability are decoupled on `widened_gradient`** — combined two-knob relaxation (`starting_energy=100, base_metabolic_cost=0.10`) extended C tick-200 living_share to ~87.5% but produced 0/64 reachability. v0.53h (N_TICKS extension on `widened_gradient` under combined SE100/BMC010) is the locked next slice — likely needs a small additive `src/` seam for `n_ticks` override.

## Where we left off

Four slices shipped end-to-end this session, all squash-merged to main. v0.53d (`WIDENED_BELOW_REACHABILITY_THRESHOLD_UNDER_RELAXED_INFLUX` — ambient_influx mechanically inert), v0.53e (`RELAXED_OPPOSITE_SIGN_HALT` — starting_energy=100 extended survival ~9% but locked-sign halt fired on a measurement-edge wrong-sign cell at n=6 with reachability=0; methodological lesson logged), v0.53f (`WIDENED_BELOW_REACHABILITY_THRESHOLD_UNDER_BASE_METABOLIC_COST_010` — base_metabolic_cost=0.10 extended survival ~16%; reachability-gated priority-3 trigger introduced and validated on the v0.53e-style edge case), v0.53g (`WIDENED_BELOW_REACHABILITY_THRESHOLD_UNDER_COMBINED_SE100_BMC010` — combined two-knob extended survival ~87.5% but reachability still 0/64; survival-extension and food-reachability decoupled). One additive `src/` seam landed in v0.53e (`body_config: BodyConfig | None = None` in `run_chamber()`); science-core five files unchanged since v0.52b. Pytest at handoff: 1808 passed, 7 skipped. Smoke ok. No `src/` work in flight. Working tree clean.

## What this session shipped

- v0.53d — `WIDENED_BELOW_REACHABILITY_THRESHOLD_UNDER_RELAXED_INFLUX` (PR #55, squash `7a456ec`); ambient_influx_rate=2.0 mechanically inert under no-food-contact extinction; B and C byte-identical.
- v0.53e — `RELAXED_OPPOSITE_SIGN_HALT` (PR #56, squash `70eb8fd`); first src/ change since v0.52b — additive `body_config` seam in `fear_hunger_chamber.py`; chamber driver SHA locked at `62d134c5d82b031a6fd2b7bbdf0412eb8362199c7bf60a59836cfca9134e2b6d`.
- v0.53f — `WIDENED_BELOW_REACHABILITY_THRESHOLD_UNDER_BASE_METABOLIC_COST_010` (PR #57, squash `d77ce35`); reachability-gated priority-3 trigger introduced (forward-looking design improvement informed by v0.53e); validated on the v0.53e-style edge case (priority 3 SKIPPED under reachability < 0.25; descriptive log section populated).
- v0.53g — `WIDENED_BELOW_REACHABILITY_THRESHOLD_UNDER_COMBINED_SE100_BMC010` (PR #58, squash `ec2add2`); first multi-knob slice; central finding (survival ⊥ reachability).

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.53d.md]] — pre-reg + Results; verdict `WIDENED_BELOW_REACHABILITY_THRESHOLD_UNDER_RELAXED_INFLUX`; mechanically-inert finding.
- [[docs/experiments/fear_hunger_v0.53e.md]] — pre-reg + Results; verdict `RELAXED_OPPOSITE_SIGN_HALT`; methodological lesson logged.
- [[docs/experiments/fear_hunger_v0.53f.md]] — pre-reg + Results; verdict `WIDENED_BELOW_REACHABILITY_THRESHOLD_UNDER_BASE_METABOLIC_COST_010`; reachability-gated priority-3 introduced + validated.
- [[docs/experiments/fear_hunger_v0.53g.md]] — pre-reg + Results; verdict `WIDENED_BELOW_REACHABILITY_THRESHOLD_UNDER_COMBINED_SE100_BMC010`; survival ⊥ reachability central finding.
- [[src/hedonism_harness/experiments/fear_hunger_chamber.py]] — v0.53e seam: `body_config: BodyConfig | None = None` parameter + default-preserving fallback `body_cfg = body_config if body_config is not None else BodyConfig()`. SHA `62d134c5...`.
- [[scripts/v0_53d_substrate_axis_relaxed_influx_audit.py]] / [[scripts/v0_53e_substrate_axis_starting_energy_audit.py]] / [[scripts/v0_53f_substrate_axis_base_metabolic_cost_audit.py]] / [[scripts/v0_53g_substrate_axis_combined_se100_bmc010_audit.py]] — reducers (1304 / 1310 / 2435 / 2499 lines).
- [[tests/test_v0_53d_substrate_axis_relaxed_influx_audit.py]] / [[tests/test_v0_53e_substrate_axis_starting_energy_audit.py]] / [[tests/test_v0_53f_substrate_axis_base_metabolic_cost_audit.py]] / [[tests/test_v0_53g_substrate_axis_combined_se100_bmc010_audit.py]] — 16 tests each (843 / 892 / 1225 / 1266 lines).

## Decisions locked in

- **Reachability-gated priority 3** (introduced v0.53f, carries forward indefinitely): priority 3 fires iff C sub-verdict = OPPOSITE_SIGN_HALT AND `c_reachability_tick_200 ≥ 0.25`. Wrong-sign cells under reachability < 0.25 logged in `audit_summary.csv` under `wrong_sign_cells_under_reachability_below_threshold` section; rollup falls through to priority 5. v0.53e's locked priority cascade stands unchanged (forward-looking design improvement, NOT retroactive).
- **`body_config: BodyConfig | None = None` seam in `run_chamber()`** (introduced v0.53e). Default-preserving (`body_cfg = body_config if body_config is not None else BodyConfig()`). SHA `62d134c5d82b031a6fd2b7bbdf0412eb8362199c7bf60a59836cfca9134e2b6d`. Test #8 Part B pins this hash going forward.
- **Test #8 splits** (introduced v0.53e): Part A pins science-core five files at v0.52b-tip SHA-256 (sensors, traits, body, config, layouts); Part B pins chamber driver at v0.53e-tip SHA-256.
- **Survival-extension and food-reachability are decoupled on `widened_gradient`** (v0.53g central finding). Bounded claim: under V0_25 × widened_gradient × N_TICKS=200, reachability is not restored by the tested conservative single- or two-knob founder-facing budget relaxations. NO "geometry-fundamental" / "budget-irrelevant" / "widened_gradient-impossible" claims.
- **Three-knob founder-budget co-variation deferred** per user's pivot call after v0.53g. The combined budget result (~87.5% survival, 0/64 reachability) is strong enough to pivot away from budget-axis exploration.
- **`body.config` access path** — `AgentBody` (frozen dataclass) does NOT expose a `.config` attribute. body_config is held at `model.body_config`. v0.53f / v0.53g reducers read via `model.body_config.<field>`. Pre-reg test #5 wording said "body.config" but the model-level access is the correct interpretation.

## Open questions / blockers

- User has not locked v0.53h details. Recommended candidate: **N_TICKS extension on `widened_gradient` under combined SE100/BMC010**. Tests whether the bottleneck is the 200-tick simulation horizon vs structural (policy / geometry / unmodeled dynamics).
- v0.53h likely needs a small additive `src/` seam to expose `n_ticks` as a `run_chamber()` override (analogous to v0.53e's body_config seam). Test #8 Part B would re-pin chamber driver at a NEW v0.53h-tip SHA. Open: dose for N_TICKS extension (e.g., 400, 800, 1000); whether to keep combined SE100/BMC010 envelope on C; whether to introduce a non-budget alternative (geometry / policy probe) as v0.53i in parallel.
- No external state blockers. No pending PRs.

## Next concrete step

**`/resume` first, then await user direction on v0.53h scope.** The recommended slice is N_TICKS extension on `widened_gradient` under combined SE100/BMC010 — but the dose, the multi-axis question (just N_TICKS, or N_TICKS + something else), and the `src/` seam shape need user lock before pre-reg. When the user names dose + scope, follow the established cadence: draft pre-reg per the v0.53e/f/g template, commit pre-reg, ask for thumbs-up on locked phrases (must reference v0.53c/d/e/f/g — predecessor stack now five long), then implement (potentially src/ seam first → reducer → 16 tests → CI gate → reducer run → Results section → commit + push → PR → merge after user confirms CI green). For the seam: `n_ticks: int | None = None` parameter to `run_chamber()` with default-preserving `n_ticks = n_ticks if n_ticks is not None else N_TICKS_DEFAULT`. Test #5 should verify the override propagates (founder still draws via `setup_observer` snapshot at tick-0; verify `model.config.n_ticks` or equivalent at the appropriate location). Test #8 Part B re-pin to NEW v0.53h-tip chamber driver SHA. Cut work onto `claude/review-hedonism-harness-Ryyx0` (the harness-locked branch — DO NOT cut a new branch unless user authorizes).

## Watch-outs

- **Branch is `claude/review-hedonism-harness-Ryyx0`**, not main. Force-push with `--force-with-lease` is the canonical post-squash-merge cleanup pattern (used four times this session: after PRs #55, #56, #57, #58).
- **One additive `src/` change since v0.52b**: v0.53e seam in `fear_hunger_chamber.py`. Science-core five files (sensors, traits, body, config, layouts) byte-identical to v0.52b-tip. Test #8 splits enforce both pinning categories.
- **Reachability-gated priority 3 is the standard pattern** going forward. v0.53h's pre-reg should carry it forward unchanged. The framework was validated on live data in v0.53f and not exercised on live data in v0.53g (but synthetic test fixture exercises the code path in test #13).
- **Locked phrases reference predecessor verdicts by name** — predecessor stack is now FIVE slices long (v0.53c/d/e/f/g). v0.53h's locked phrases must include all five.
- **DO NOT claim "geometry is fundamental" or "budget is irrelevant"** under any v0.53h priority-5 outcome. The combined-budget result extended survival ~9× — budget is dramatically responsive on survival. The empirical claim is bounded: reachability is not restored by the tested envelopes.
- **Per-PR cadence**: pre-reg first commit, implementation+Results second commit (or implementation second, Results third for slices with substantive Results sections). Each of v0.53d/e/f/g followed this pattern.
- **CI gate before push**: ruff + format + pytest + smoke. Smoke test (`scripts/core_smoke_test.py`) is the determinism north star — MUST continue to pass after any `src/` seam-add.
- **`body.config` is a pre-reg wording shorthand**, not a literal API path. The actual access path is `model.body_config.<field>`. Future pre-regs should write the literal path.
- **Three-knob founder-budget co-variation deferred** per user's v0.53g pivot. If user changes mind, the natural slice is `BodyConfig(starting_energy=100, base_metabolic_cost=0.10, max_energy=200)` — but it requires raising `max_energy` (currently default 100), which interacts with `starting_energy ≤ max_energy` Pydantic validator.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok (236 files already formatted)
uv run pytest                   1808 passed, 7 skipped
uv run python scripts/core_smoke_test.py   ok (default behavior preserved; one src/ seam since v0.52b is default-preserving)
```
