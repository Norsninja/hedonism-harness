# Handoff: v0.26 + v0.27 shipped; v0.28 food_ladder trajectory diagnostic planned

**Date:** 2026-05-05
**Branch:** `claude/v0.28-food-ladder-trajectory-diagnostic` (cut from main tip `9ff9917`)
**Tip commit:** `9ff9917`
**Status:** v0.26 + v0.27 merged to main this session. v0.28 pre-reg + implementation not yet started.

## Where we left off

Two PRs shipped this session: v0.26 (#21 → `6cb2103`) and v0.27 (#22 → `9ff9917`). v0.26 introduced the `hazard_avoidance_weight` seam in `GradientPolicy` (Reading A — pure policy multiplier on `pain_w`, default 1.0 = no-op). v0.27 mapped the avoidance-weight axis at five points on both chambers. **Both chambers exhibit interior optima on the avoidance-weight axis**: tight w*=0.75 (weak, +2 b>50 vs w=1.0 — exactly meeting the noise threshold), food_ladder w*=0.5 (decisive, +6 b>50). Routing observables are monotone but productivity is not — over-routing reading. **Open anomaly:** food_ladder b>50 dips sharply at w=0.75 (89, vs 98 at w=0.5 and 92 at w=1.0). Aggregate flag, not yet a stable regime.

## What this session shipped

- v0.26 PR #21 squash-merged: `hazard_avoidance_weight` GradientPolicy seam + 2×2 grid; phantom ≡ no-hazard byte-identical (Reading A confirmed); avoidance routing load-bearing on BOTH chambers (food_ladder geometry-as-buffer reading falsified).
- v0.27 PR #22 squash-merged: avoidance-weight frontier (5 weights × 2 chambers × 8 seeds = 80 runs in 22.2s); both chambers have interior optima; food_ladder w=0.75 dip flagged for v0.28.
- Test suite 705 → 716 (+11 v0.27 tests; v0.26 brought 687 → 705).
- Pre-merge audit discipline: H8 / H10 / H11 wording tightened on user notes before each merge.

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.26.md]] — pre-reg + results; 2×2 grid; Reading A confirmed.
- [[docs/experiments/fear_hunger_v0.27.md]] — pre-reg + results; both-chamber interior optima; food_ladder w=0.75 dip.
- [[src/hedonism_harness/policies/gradient_policy.py]] — `hazard_avoidance_weight` parameter (default 1.0; multiplies `pain_w`).
- [[src/hedonism_harness/experiments/comparison_grid.py]] — `Arm.hazard_avoidance_weight` field; `V0_26_ARMS` (4); `V0_27_ARMS` (5).
- [[src/hedonism_harness/experiments/fear_hunger_chamber.py]] — `run_chamber` threads weight via factory wrapping (default None preserves bit-identity).
- [[src/hedonism_harness/experiments/population_dynamics.py]] — v0.24 library, **re-usable by v0.28** for trajectory analysis.
- [[scripts/v0.26_sweep.py]], [[scripts/v0.27_sweep.py]] — sweep drivers.

## Decisions locked in

- **Reading A** (pure policy seam — `hazard_avoidance_weight` multiplies `pain_w`) is sufficient for the perception-vs-damage decoupling questions raised so far. Reading B (separate `world.hazard_perceived` layer) deferred until a question explicitly needs `phantom != no-hazard`.
- **Avoidance over-routing** is the working mechanism for both chambers' interior optima on the weight axis. Symmetric to v0.25's tight-interior-optimum-at-h=8 on the hazard axis.
- **v0.28 = food_ladder trajectory diagnostic** at w ∈ {0.5, 0.75, 1.0}, no new sweep beyond v0.27 artifacts unless the diagnostic determines one is needed. Per-version branch cadence continues.

## Open questions / blockers

- **none blocking.** v0.28 is a diagnostic on existing v0.27 events.jsonl; no pre-reg gating needed beyond the standard discipline.
- **Remote branches** `claude/v0.26-perception-vs-damage-decoupling` and `claude/v0.27-avoidance-weight-frontier` could not be deleted from inside the sandbox (proxy returns HTTP 403 on `git push :branch`). Local refs are gone; remote refs need manual cleanup outside the sandbox.

## Next concrete step

**Write the v0.28 pre-reg first**, then build the diagnostic. The slice is a per-seed trajectory analysis on food_ladder events.jsonl from the v0.27 sweep at w ∈ {0.5, 0.75, 1.0} (24 runs already on disk in `runs/fear-hunger-v0.27-food_ladder/arms/hzd8-avd0.50/`, `.../hzd8-avd0.75/`, `.../hzd8-avd1.00/`). Use `population_dynamics.load_population_trajectory` from v0.24 to extract per-seed: b>50, tick_of_peak, population time series, pool_min_tick, birth-tick distribution, food consumption by 50-tick band, hazard entries by 50-tick band, death causes by 50-tick band. Goal: explain the w=0.75 dip — is it a routing-threshold flip (some seeds route through a worse corridor at w=0.75)? a population-dynamics artefact (specific seeds crash mid-run)? a chamber-geometry interaction? The pre-reg should commit candidate signatures before reading the data. **Do not expand the (hazard, weight, influx) parameter grid until the dip is explained.** Drive script: new `scripts/v0.28_trajectory_diagnostic.py`. Library extension only if needed (e.g., per-tick hazard-entry / food-event aggregation that v0.24's `PopulationTrajectory` doesn't already expose).

## Watch-outs

- **Determinism:** `population_dynamics` library currently exposes `population_at_tick`, `lifespans`, `starvation_deaths_per_tick`, `injury_deaths_per_tick`. If v0.28 needs per-tick food / hazard / births, extend the library purely-functionally (no mutation; same byte-identity discipline as v0.24).
- **Aggregate-vs-per-seed framing.** v0.27 doc explicitly flagged the w=0.75 dip as "aggregate large enough to flag, not a stable regime." Per-seed inspection is the diagnostic; do NOT publish "the dip is real" framing without seed-level evidence.
- **food_ladder w=0.5 (b>50=98) is the food_ladder over-routing optimum, not the dip site.** The diagnostic compares dip (w=0.75) against neighbors (w=0.5, w=1.0) using the SAME 8 seeds — paired comparisons.
- **No new sweep is mandatory.** The v0.27 events.jsonl artifacts at w ∈ {0.0, 0.25, 0.5, 0.75, 1.0} are on disk. Only run a new sweep if the per-seed analysis reveals a need (e.g., test reproducibility on seeds 9..16, ~5s for one chamber).
- **Test suite is at 716.** Pure-additive v0.28 should bring it to ~725–735 depending on diagnostic complexity.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   716 passed
uv run python scripts/core_smoke_test.py  ok
```
