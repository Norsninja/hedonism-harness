# Handoff: v0.9 memory arm pending

**Date:** 2026-05-04
**Branch:** `main`
**Tip commit:** `34902f0`
**Status:** v0.7 + v0.7b + v0.8 + 2 hygiene slices merged via PR #3. First reproducing population achieved (ladder-sr1: 5 births / 4 seeds). v0.9 memory arm is the next move.

## Where we left off

`main` carries six new commits since the prior handoff (`reproduction-emergence-pending`), all merged via [PR #3](https://github.com/Norsninja/hedonism-harness/pull/3): v0.7 reproduction-config grid (no formal winner, threshold-positive diagnostic), v0.7 ReproductionRequested signal bug fix, v0.7b motivation grid (drive dormant), v0.8 structural eligibility rescue (winner: `ladder-sr1`), hygiene-1 (spawn safety + cell_at + effort pin), hygiene-2 (layout invariants + spread_y dedup + telemetry timing). 367 tests passing. Diagnostic ladder confirmed: parameter-only blockers (cost, drive) falsified; structural change (food_ladder + sensor_radius_min=1) produces robust reproduction. The tight_gradient chamber remains the "hard problem" — sensor widening alone did not rescue it. **v0.9 memory arm is the next test for that chamber, and is unblocked.**

## What this session shipped

- v0.7 reproduction grid [[docs/experiments/fear_hunger_v0.7.md]] — 9-cell (et × ec) sweep; falsified "cost is blocker"; 0/9 qualifiers; threshold-positive (et=50 unlocks 1 birth)
- v0.7 metrics bug fix [[src/hedonism_harness/mesa_agents.py]] — ReproductionRequested signal now flows through record_event; tally.reproduction_requests was permanently 0 across project history
- v0.7b motivation grid [[docs/experiments/fear_hunger_v0.7b.md]] — 9-cell (et × drive_min) sweep; falsified "drive is blocker"; 0/9 qualifiers; new metrics births_per_request and seeds_with_reproduction_requests pin the diagnosis
- v0.8 eligibility rescue [[docs/experiments/fear_hunger_v0.8.md]] — 9-cell (layout × sensor_radius_min) sweep; **winner ladder-sr1 with 5 births / 4 seeds**; first reproducing population
- v0.8 telemetry [[src/hedonism_harness/experiments/eligibility_telemetry.py]] — 7 diagnostic fields (eligible_agent_ticks, max_energy_after_min_age, median_death_age, ...) via setup_observer + tick_observer hooks
- Hygiene-1: capacity=1 spawn safety, HHModel.cell_at seam, valence effort double-channel pin
- Hygiene-2: ChamberLayout pre-food invariants, _spread_y → spread_y rename + dedup across 6 snapshot helpers, setup_observer kwarg on run_chamber (closes tick-0 telemetry trap)
- Tests: 283 → 367 (+84) across the slice; v0.8 sweep verified bit-identical post-hygiene-2

## Files touched (wikilinks for fast load)

- [[src/hedonism_harness/mesa_agents.py]] — emits ReproductionRequested via record_event before queueing the birth (bug fix)
- [[src/hedonism_harness/model.py]] — `HHModel.cell_at(x, y)` public seam over Mesa's private `_cells`
- [[src/hedonism_harness/experiments/fear_hunger_chamber.py]] — `ChamberLayout.__post_init__` (pre-food invariants), `spread_y` (renamed, ValueError on n>height), `setup_observer` kwarg
- [[src/hedonism_harness/experiments/layouts.py]] — `widened_gradient_layout`, `food_ladder_layout`
- [[src/hedonism_harness/experiments/trait_configs.py]] — `tuned_trait_config`, `motivation_trait_config`, `eligibility_trait_config`
- [[src/hedonism_harness/experiments/repro_configs.py]] — `tuned_reproduction_config(*, energy_threshold, energy_cost)`
- [[src/hedonism_harness/experiments/repro_grid.py]] — v0.7 driver
- [[src/hedonism_harness/experiments/motivation_grid.py]] — v0.7b driver, MotivationCellAggregate w/ births_per_request + seeds_with_reproduction_requests
- [[src/hedonism_harness/experiments/eligibility_grid.py]] — v0.8 driver, EligibilityCellAggregate (joint result+telemetry), 6-criterion v0.7b rule applied unchanged
- [[src/hedonism_harness/experiments/eligibility_telemetry.py]] — `EligibilityTelemetryCollector`, polled per-tick + signal subscribers
- [[tests/test_repro_grid.py]] / [[tests/test_motivation_grid.py]] / [[tests/test_eligibility_grid.py]] / [[tests/test_model_cell_at.py]] — new test suites (16 + 23 + 26 + 1)
- [[tests/test_newborn_defer.py]] — 2 regression tests pinning ReproductionRequested signal flow
- [[tests/test_layouts.py]] / [[tests/test_experiments_fear_hunger.py]] / [[tests/test_valence.py]] — extended for new layouts, spread_y safety, effort double-channel

## Decisions locked in

- **v0.9 trait/repro/layout config (per pre-registered v0.8 next-step):** Hold reproduction params at v0.7 threshold-positive cell (`et=50, ec=35`), trait base at v0.6 winner with `drive_min=0.0`, sensor_radius range at SPEC default. Sweep `MemoryHedonismPolicy` parameters and revisit the *tight_gradient* chamber. The food_ladder cell is "easy mode"; tight_gradient is the open question for v0.9.
- **Strict pre-registration discipline preserved.** Two failed sweeps (v0.7, v0.7b) reported as no-formal-winner with diagnostic readings; criterion 2 (food preservation) corrected to `food >= baseline − births` for v0.7b onward to absorb the structural one-tick time-cost.
- **Only two narrow core-adjacent changes** in the entire merged slice: (1) `mesa_agents.py` emits ReproductionRequested through record_event (bug fix); (2) `HHModel.cell_at` wrapper. Everything else lives in `experiments/` per SPEC §27.11.
- **Effort accounting ablation deferred.** Current behavior is double-channel (`c × (1 + hunger_pain_sensitivity)`); pinned by `test_action_energy_cost_is_double_channelled_via_pain_and_effort`. See [[docs/experiments/fear_hunger_v0.8.md]] § Deferred work.

## Open questions / blockers

- None blocking. User explicitly endorsed v0.9 as next.

## Next concrete step

Cut `claude/review-handoff-docs-XVPTd` (or whatever session-mandated branch the next session receives) from `main` at `34902f0`. Implement [[src/hedonism_harness/policies/memory_hedonism_policy.py]] (new) — a ``MemoryHedonismPolicy`` that maintains a per-agent `ValenceMemory` keyed by (x, y) and adds `remembered_good_*` / `remembered_bad_*` to the observation. The valence harness already plumbs these signals (see [[src/hedonism_harness/core/valence.py]] `_remembered_good_total` / `_remembered_bad_total`); they currently read 0 because no policy populates the memory. Test plan: a ``MemoryHedonismPolicy`` smoke + a v0.9 sweep mirroring v0.8's structure but on the *tight_gradient* chamber (the unsolved hard problem). Pre-register v0.7b's 6-criterion rule unchanged. Driver location: [[src/hedonism_harness/experiments/memory_grid.py]] (new). Sweep axes: probably `memory_strength_min ∈ {0.0, 0.5, 1.0}` × `memory_decay_rate_max ∈ {0.0, 0.05, 0.1}` (the SPEC §8.1 master ranges); discuss with user before running. Run on tight_gradient + `eligibility_trait_config(sensor_radius_min=1)` + `tuned_reproduction_config(energy_threshold=50, energy_cost=35)`. Target: ≥2 cells qualify, demonstrating that memory rescues tight_gradient.

## Watch-outs

- **v0.6 winner trait config is the project default for experiments now** — see [[src/hedonism_harness/experiments/trait_configs.py]] `tuned_trait_config(fear_max=1.5, hunger_min=1.0, risk_min=0.55)`. Don't accidentally use `default_trait_config()` (SPEC defaults) without a stated reason.
- **The food_ladder layout has a pre-food band at x=4** that bypasses the hazard barrier. v0.9 must run on `tight_gradient` (no pre-food, hazard wall intact) to make the memory question meaningful. The food_ladder result is "easy mode" — keep it as a positive-control reference point only.
- **`max_population_end <= 15` overpopulation guard** is hard-coded for `n_founders=5`. If the next session changes founder count, update [[src/hedonism_harness/experiments/eligibility_grid.py]] `OVERPOPULATION_MULTIPLIER` semantics or pass `n_founders` through.
- **Determinism north star**: [[scripts/core_smoke_test.py]] + [[tests/test_simulation_determinism_mesa.py]]. Both passed at handoff time. v0.8 sweep was bit-identical pre/post-hygiene-2 — that determinism check should be repeated after any v0.9 core change.
- **`tally.reproduction_requests`** now works correctly (was permanently 0 pre-v0.7); criterion 6 (`reproduction_requests >= total_births`) is structurally guaranteed but kept as a defense-in-depth check.
- **`setup_observer` fires AFTER `paint_chamber` + aggregator.connect() but BEFORE the first `model.step()`.** Future telemetry collectors should use this hook (not lazy init in tick_observer) to avoid silently dropping tick-0 events.
- **Branch protocol**: the merged `claude/review-handoff-docs-XVPTd` branch still exists locally and on origin (per "do not delete without direction"). The next session may receive a different mandated branch name; cut from `main` at `34902f0` regardless.
- Project ladder (post-v0.9): v0.10 lineage-depth analysis (do gen-2 children themselves reproduce?); v0.11 sensor expansion if memory alone doesn't suffice.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   367 passed
uv run python scripts/core_smoke_test.py  ok
```
