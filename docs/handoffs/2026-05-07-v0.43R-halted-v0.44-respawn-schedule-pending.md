# Handoff: v0.43R halted post-sweep; v0.44 respawn-schedule probe pending

**Date:** 2026-05-07
**Branch:** `claude/v0.43-resource-flattening-intervention`
**Tip commit:** `384c9c8`
**Status:** Two consecutive substrate-mismatch halts; v0.44 feasibility probe operative; v0.44 pre-reg pending user signoff on locked design.

## Where we left off

v0.43R halted post-sweep with `SUBSTRATE_PREFLIGHT_HALT_2`. The audit emitted `FOOD_DENSITY_NOT_NECESSARY` mechanically because the substrate at tick 50 is **completely depleted** (total_food=0 across all 48 runs at both hazards) — the verdict is **disqualified** as no-op output, not cited. The earlier "saturated zone" claim in v0.43's halt addendum is **retracted** — that probe used a hand-rolled `run_chamber` invocation that defaulted to `HedonismPolicy` + `auto_reproduction=False`, which doesn't match the V0_25 anchor's actual sweep config (`GradientPolicy` + `auto_reproduction=True` + `TraitConfig(unbounded_mutation=True)`). Methodological lesson locked: **always feasibility-probe through `_run_one_arm_seed`**, never through hand-rolled `run_chamber`. v0.44 candidate (respawn-schedule intervention) feasibility probe ran and confirmed operative substrate (24 cells scheduled at tick 50, refill ticks heterogeneous across 54..67). v0.44 pre-reg sketch surfaced; awaiting signoff on three design questions before drafting.

## What this session shipped

- v0.43 SUBSTRATE_PREFLIGHT_HALT addendum (then retracted) — `[[docs/experiments/fear_hunger_v0.43.md]]`.
- v0.43R pre-reg + 4-arm primitives + sweep + audit + paired tests (83 new tests).
- v0.43R sweep + audit ran (no-op data; verdict disqualified by halt).
- v0.43R SUBSTRATE_PREFLIGHT_HALT_2 addendum + retraction notice on v0.43's halt.
- v0.44 feasibility probe (operative): 24 scheduled cells per run, refill ticks 54..67 across all seeds 49..56.
- 1414 tests pass, 7 skipped throughout.

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.43.md]] — pre-reg + halt addendum + retraction notice (saturated-zone claim retracted; preserved as historical record).
- [[docs/experiments/fear_hunger_v0.43R.md]] — pre-reg + Results-as-halt + SUBSTRATE_PREFLIGHT_HALT_2 with corrected substrate finding + methodological lesson + v0.44 candidate sketch.
- [[src/hedonism_harness/core/interventions.py]] — extended with `KIND_REDUCE_DENSITY_50PCT`, `KIND_DENSITY_PRESERVING_PERTURBATION`, helpers, dispatcher.
- [[src/hedonism_harness/experiments/comparison_grid.py]] — `V0_43R_INTERVENTION_ARMS` + `_v0_43r_arm` helper.
- [[scripts/v0.43r_sweep.py]] — sweep driver.
- [[scripts/v0_43r_intervention_audit.py]] — audit reducer (verdict emitted but disqualified).
- [[tests/test_interventions_v0_43r.py]] — 24 unit tests.
- [[tests/test_world_intervention_hook_v0_43r.py]] — 12 hook + regression tests.
- [[tests/test_v0_43r_sweep.py]] — 11 sweep config tests.
- [[tests/test_v0_43r_intervention_audit.py]] — 36 audit tests.
- [[tests/test_world_intervention_hook_v0_43.py]] — skip reason updated to acknowledge both substrate regimes (saturated under hand-rolled, depleted under sweep config).

## Decisions locked in

- **v0.43R verdict disqualified.** `FOOD_DENSITY_NOT_NECESSARY` was emitted mechanically from a no-op intervention; do NOT cite as a scientific result.
- **v0.43 saturated-zone claim retracted** — was config-mismatched; corrected finding lives in v0.43R's halt addendum.
- **Methodological lesson locked**: feasibility probes MUST run through `_run_one_arm_seed` or `run_comparison_grid`, never through hand-rolled `run_chamber` (the latter omits `arm.policy_factory`, `auto_reproduction` setup_observer, and `TraitConfig(unbounded_mutation=True)`).
- **Corrected substrate finding (live)**: under V0_25 anchor (GradientPolicy + auto_reproduction + heavy mutation), the 24-cell food zone is **completely depleted** at tick 50 across all seeds 49..56 at both hazards. Post-50 dominance reads off respawn-flow timing, not static food density.
- **v0.44 direction**: respawn-schedule intervention. Three arms parallel to v0.42/v0.43R: A_null / B_delay_respawn_schedule / C_permute_respawn_schedule.
- **v0.43/v0.43R intervention infrastructure preserved** (kill, flatten, shuffle, reduce-density, perturbation kinds remain dispatchable for future probes; v0.42 byte-identity invariants hold via H2e regression in [[tests/test_world_intervention_hook_v0_43r.py]]).

## Open questions / blockers

- **v0.44 design — three locked questions awaiting user signoff:**
  - **B_delay magnitude**: +25 ticks (recommended; halves post-50 refill rate over the 50-tick window) vs +50 vs +100.
  - **Seeds**: 57..64 (next disjoint band; recommended).
  - **New event class** `RespawnScheduleByIntervention` (recommended; semantically distinct from `FoodRedistributedByIntervention`) vs reuse existing.
- C_permute scope question collapsed to "all scheduled cells" — probe confirmed all 24 cells satisfy `respawn_at_tick > 51` at tick 50.

## Next concrete step

**Draft v0.44 pre-reg** (`docs/experiments/fear_hunger_v0.44.md`) on the same branch, after user signs off on the three open questions. Locked components from this session's probe: tick 50/51 anchor; hazards {0, 8}; eligible-cell predicate `kind ∈ {EMPTY, FOOD} AND respawn_at_tick > 0` (24 cells/run); C_permute = reverse-row-major over `(x, y)`-sorted scheduled cells (multiset of refill ticks preserved); primary observable `post_intervention_top_lineage_b50_share` reused; threshold structure (B drop ≥ 0.15, |C−A| ≤ 0.10) reused; verdicts `RESPAWN_RATE_NECESSARY` / `SUBSTRATE_PERTURBATION_DISRUPTS_DOMINANCE` / `RESPAWN_RATE_NOT_NECESSARY` + halt cell `C_ABOVE_A_UNMODELED_SUBSTRATE_ARTEFACT`; auxiliary `DELAY_ABLATES_REPRODUCTION` per hazard. Implementation extends `core/interventions.py` with two new kinds + helpers; needs new event class iff user picks that option. Probe data for refill-tick distribution at tick 50 is in this conversation's tail — re-derive via `_run_one_arm_seed` if needed.

## Watch-outs

- **Do NOT cite v0.43R's `FOOD_DENSITY_NOT_NECESSARY` verdict** — it's a no-op mechanical output, disqualified per SUBSTRATE_PREFLIGHT_HALT_2.
- **Do NOT cite v0.43's "saturated zone" claim** — retracted; the substrate is depleted, not saturated.
- **`runs/` is gitignored.** Next session must re-run v0.42 sweep + audit (~13s + ~3s) to regenerate the v0.42 reference for v0.44's H2e byte-identity regression. v0.43R sweep can also be re-run (~18s) but its outputs are not scientifically citable.
- **Feasibility probes must use `_run_one_arm_seed`** — direct `run_chamber` calls produce config-mismatched substrate and led to v0.43 + v0.43R halts. The probe pattern for v0.44 lives in the conversation tail (replicates the V0_25 anchor's policy + setup_observer + trait_config).
- **v0.42 / v0.43 / v0.43R intervention kinds remain dispatchable**; they're regression-guarded by [[tests/test_world_intervention_hook_v0_43r.py]] H2e regression on `kind="null"` and `kind="kill_tick50_leader"`.
- **Three handoff predecessors exist for 2026-05-07** but none in this branch — the v0.42-shipped handoff is on `claude/handoff-v0.42-shipped` (separate branch).

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   1414 passed, 7 skipped (6 baseline +
                                1 v0.43 substrate-finding skip)
uv run python scripts/core_smoke_test.py                ok
uv run python scripts/v0.43r_sweep.py                   completed (no-op)
uv run python scripts/v0_43r_intervention_audit.py      verdict emitted but
                                                        DISQUALIFIED per halt
```
