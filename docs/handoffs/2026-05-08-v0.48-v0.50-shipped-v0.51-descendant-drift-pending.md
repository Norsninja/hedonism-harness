# Handoff: v0.48–v0.50 shipped; v0.51 descendant-drift probe pending

**Date:** 2026-05-08
**Branch:** `main`
**Tip commit:** `9a95ad2`
**Status:** Three slices over the modern A_null corpus shipped this session (v0.48 observational bridge, v0.49 first-class causal probe, v0.50 founder-position confound). v0.51 = descendant-trait-clamp probe locked at design level (3 arms, label-A-degenerate-in-B-and-C, two-tier re-anchor, categorical rollup); birth-pipeline investigation done; pre-reg drafting pending user go-ahead.

## Where we left off

Five PRs merged this session: v0.46 (#43, `d87dbe2`), v0.47 (#44, `db128b1`), v0.48 (#45, `18cb5af`), v0.49 (#46, `207a433`), v0.50 (#47, `9a95ad2`). The evidence stack is now: v0.47 finds founder `sensor_radius` is the only firing primary trait predictor of tick-50 readiness fraction; v0.48 establishes the spatial / foraging bridge under both labels (3/3 fire, ROBUST under v0.50); v0.49 fires `SENSOR_RADIUS_CAUSAL_CONTRIBUTION_SUPPORTED` via founder clamp + permutation; v0.50 fires `SENSOR_RADIUS_ROBUST_TO_POSITION` (all 18 cells fire). v0.51 design is locked at the verdict-structure level. User explicitly paused before drafting v0.51's pre-reg; awaiting "proceed" signal to cut `claude/v0.51-descendant-drift-probe` from main and draft `docs/experiments/fear_hunger_v0.51.md`.

## What this session shipped

- v0.48 — `SENSOR_RADIUS_SPATIAL_BRIDGE_PRESENT`. Pre-reg + reducer + 14 tests. PR #45.
- v0.49 — `SENSOR_RADIUS_CAUSAL_CONTRIBUTION_SUPPORTED`. First-class intervention slice. Pre-reg + reducer + 17 tests. PR #46.
- v0.50 — `SENSOR_RADIUS_ROBUST_TO_POSITION`. Founder-position confound probe + trait-covariance preflight. Pre-reg + 2 scripts + 16 tests. PR #47.
- Two pre-implementation corrections logged: v0.49 (RNG-stream bug → patch in `setup_observer` after `HHModel(...)` returns); v0.50 (B arm is shifted-top reflection, NOT centered; MIXED rollup made reachable via 9-cell partition).
- Birth-pipeline investigation confirmed Option 1 (post-`AgentBorn` listener patch) safe for v0.51's descendant-trait-clamp arm.

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.48.md]] — bridge present; 6/6 cells fire under both labels.
- [[docs/experiments/fear_hunger_v0.49.md]] — first-class causal probe; clamp + permutation arms.
- [[docs/experiments/fear_hunger_v0.50.md]] — founder-position confound probe; shifted-top + permutation; ROBUST.
- [[scripts/v0_48_sensor_radius_spatial_bridge_audit.py]] — v0.48 reducer.
- [[scripts/v0_49_sensor_radius_causal_probe_audit.py]] — v0.49 reducer; trait patch via `setup_observer` body-trait replace.
- [[scripts/v0_50_founder_trait_covariance_preflight.py]] — preflight; max |ρ|=0.144.
- [[scripts/v0_50_founder_position_audit.py]] — v0.50 reducer; two-phase position patch (detach all, then re-attach) for capacity-1 grid.

## Decisions locked in

- **v0.51 arms (locked, 3)**: A_null, B_founder_clamp_4 (continuity with v0.49), C_lineage_clamp_4 (founder + descendant clamp via `AgentBorn` listener).
- **v0.51 gating (locked)**: A_null gates Label A AND Label B; **B and C gate Label B only** (label A degenerates under both clamp arms — 5-way tie, `min(lineage_id)` always wins). CSV adds `label_a_gating_valid=False`/`label_a_degenerate_reason="all founders assigned sensor_radius=4"` for B and C rows.
- **v0.51 two-tier re-anchor**: Tier 1 (priority-3 halt) = A_null vs v0.48's six published cells; Tier 2 (new priority-4 halt = `FOUNDER_CLAMP_REPLICATION_HALT`) = B vs v0.49 B cells (label A diagnostic: −0.243/−0.243/−0.732; label B gating: +0.182/+0.182/−0.181). Both within 1e-3.
- **v0.51 rollup (categorical-only, no magnitude-delta rule)**: 4 outcomes priority-ordered after halts: `FOUNDER_CLAMP_REPRODUCED` (A=PRESENT, B=NOT_FOUND, C=NOT_FOUND); `LINEAGE_CLAMP_ADDITIONAL_WEAKENING` (PRESENT, PRESENT, NOT_FOUND); `LINEAGE_CLAMP_NO_ADDITIONAL_WEAKENING` (PRESENT, PRESENT, PRESENT); `LINEAGE_DRIFT_MIXED` (catch-all). Locked phrases drafted; full text in chat history pre-handoff.
- **v0.51 implementation = Option 1 (post-`AgentBorn` listener)**: confirmed safe by sub-agent investigation. Births at phase 7 of `model.step()` (after metabolism/hazard); blinker `send()` synchronous; child reachable via `model.agents` immediately after Mesa registration; listener fires before `del child_agent`. Newborns first step on next tick. Caveats: `model.trait_fingerprints` records pre-patch (don't use it; capture own audit per v0.49/v0.50 pattern); listener closure must be retained in `disconnect_callbacks` for strong-reference.
- **No `src/` modifications** preserved across v0.51 by Option 1 path.

## Open questions / blockers

- User said "I would not draft v0.51 yet" before locking the corrections; corrections are now locked. Awaiting explicit "proceed" to cut `claude/v0.51-descendant-drift-probe` and start drafting.
- Magnitude-delta rule for B-vs-C signed_d differences explicitly **not locked** per user preference (categorical-only). Report magnitude differences descriptively in Results.

## Next concrete step

Cut `claude/v0.51-descendant-drift-probe` from main at `9a95ad2`. Run `/resume` first, then draft `docs/experiments/fear_hunger_v0.51.md` with: the locked 3 arms; label-A-diagnostic-only under B AND C; two-tier re-anchor (priority 3 = A_null vs v0.48; priority 4 = `FOUNDER_CLAMP_REPLICATION_HALT` against v0.49 B cells); categorical rollup with the 4 outcomes named above; locked phrases for each; 16 locked tests including a synthetic test that drives the AgentBorn listener with a stub event and verifies single-channel patch + non-mutation of `trait_fingerprints`. Implementation = Option 1 (AgentBorn listener) for arm C; arm B reuses v0.49's setup_observer founder-clamp pattern verbatim. Conservation framing: NO `src/` modifications. Mirror v0.49's reducer structure; copy-local helpers freely. Wall time estimate: ~10 min for 192 runs.

## Watch-outs

- **Listener strong-reference**: connect via `signal_for(AgentBorn).connect(handler)` and stash `handler` in `disconnect_callbacks`. Blinker can drop weakly-referenced handlers.
- **trait_fingerprint pre-patch**: `_record_trait_fingerprint` (model.py:768-ish) runs BEFORE `AgentBorn` emit (line 778). v0.51 must not consume `model.trait_fingerprints` for the C-arm audit; capture own founder + descendant trait audit table from live `model.agents` post-patch.
- **Two-phase patch is for v0.50 (position); not needed in v0.51**: v0.51 modifies `body.traits.sensor_radius`, not `body.x/y` or Mesa cell pointers, so the v0.49 single-phase pattern applies for both founder patch and AgentBorn listener.
- **AgentBorn event contains `agent_id` only** — no trait fields. Listener must scan `model.agents` to find the agent by id; v0.51 reducer should index agents by id once and reuse if perf matters (probably not at 192 runs × O(births)).
- **B arm continuity**: copy v0.49's founder-clamp logic verbatim (same `dataclasses.replace` on `body.traits.sensor_radius`). Tier-2 re-anchor enforces byte-identity at the metric level.
- **Determinism north star** ([[scripts/core_smoke_test.py]]) must pass; v0.51 is no `src/` change so this is preserved by construction.
- **All four prior intervention paths** (v0.42, v0.43R, v0.44, v0.45) remain dispatchable on main. v0.46–v0.50 reducers byte-identical to merged forms.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   1648 passed, 7 skipped
uv run python scripts/core_smoke_test.py   ok
```
