# Handoff: v0.53 + v0.53b + v0.53c shipped; next-step open

**Date:** 2026-05-09
**Branch:** `claude/review-hedonism-harness-Ryyx0` (currently aligned with `origin/main` post-squash; future work continues here unless told otherwise)
**Tip commit:** `13d179a`
**Status:** Cross-layout-generalization arc closed within V0_25 substrate. Three slices merged this session (v0.53 #52, v0.53b #53, v0.53c #54). Layout × V0_25 axis fully scoped: `widened_gradient` bridge cannot be measured (reachability = 0 at every window 50/100/200; 100% extinction by tick-200); `tight_gradient` and `food_ladder` admit the bridge with structurally interesting tick-window dynamics. Pre-data design discussion required for whichever slice is picked next.

## Where we left off

Three slices shipped end-to-end this session, all squash-merged to main: v0.53 (`BRIDGE_PARTIALLY_GENERALIZES` — A_null PRESENT, B_widened NOT_FOUND, C_food_ladder PRESENT at tick-50), v0.53b (`WIDENED_BELOW_REACHABILITY_THRESHOLD_AT_TICK_100` — B_widened reachability = 0/64 at tick-100, 50% extinction by tick-100), v0.53c (`WIDENED_BELOW_REACHABILITY_THRESHOLD_AT_TICK_200` — B_widened reachability = 0/64 at every window, 100% extinction by tick-200). v0.53's `BRIDGE_PARTIALLY_GENERALIZES` verdict scope is now formally bounded to (V0_25 substrate × `widened_gradient`) along the layout-window axis. No `src/` changes in any of the three slices (SHA-256-pinned in test #8 of each). Pytest at handoff: 1744 passed, 7 skipped. Smoke ok. No `src/` work in flight. No uncommitted changes.

## What this session shipped

- v0.53 — `BRIDGE_PARTIALLY_GENERALIZES` (PR #52, squash `cda99bd`); 192-run cross-layout sweep (tight_gradient / widened_gradient / food_ladder); A_null observational paired_d per layout; first cross-layout slice.
- v0.53b — `WIDENED_BELOW_REACHABILITY_THRESHOLD_AT_TICK_100` (PR #53, squash `0a00ef4`); same 192-run shape with two-window observer (tick-50 anchor + tick-100 verdict); reachability metric + threshold (0.25); strict NaN-counts-toward-denominator sub-verdict rule (locked carry-over from v0.48–v0.53); relaxed contiguous-prefix tick-records invariant (handles early extinction).
- v0.53c — `WIDENED_BELOW_REACHABILITY_THRESHOLD_AT_TICK_200` (PR #54, squash `13d179a`); three-window observer (pre50 + pre100 + pre200) and three Label B variants (tick50/tick100/tick200); population-stability descriptive metrics added; tick-200 = full N_TICKS=200 simulation length.

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.53.md]] — pre-reg + Results; verdict `BRIDGE_PARTIALLY_GENERALIZES`; Reading B locked (single A_null per layout).
- [[docs/experiments/fear_hunger_v0.53b.md]] — pre-reg + Results; verdict `WIDENED_BELOW_REACHABILITY_THRESHOLD_AT_TICK_100`; B_widened-only gating; reachability metric + 25% threshold.
- [[docs/experiments/fear_hunger_v0.53c.md]] — pre-reg + Results; verdict `WIDENED_BELOW_REACHABILITY_THRESHOLD_AT_TICK_200`; population-stability descriptors; A_null Label B strengthens at tick-200; C_food_ladder Label B degrades across windows.
- [[scripts/v0_53_cross_layout_generalization_audit.py]] — 1500 lines; v0.53 reducer with layout-invariant assert.
- [[scripts/v0_53b_reachability_disambiguation_audit.py]] — 1832 lines; v0.53b reducer; two-window observer.
- [[scripts/v0_53c_reachability_disambiguation_tick200_audit.py]] — 2129 lines; v0.53c reducer; three-window observer + three Label B variants + extinction handling.
- [[tests/test_v0_53_cross_layout_generalization_audit.py]] — 646 lines, 16 tests.
- [[tests/test_v0_53b_reachability_disambiguation_audit.py]] — 831 lines, 16 tests.
- [[tests/test_v0_53c_reachability_disambiguation_tick200_audit.py]] — 927 lines, 16 tests.

## Decisions locked in

- **Reading B locked for v0.53** (user-locked): single A_null per layout, NOT v0.49-style 3-sub-arm intervention per layout. Reading A (causal-generalization per layout) deferred to a future slice.
- **Strict NaN-counts-toward-denominator sub-verdict rule** (locked carry-over from v0.48–v0.53): a label with 3 NaN cells cannot fire PRESENT under that label. Test #12 in v0.53c enforces.
- **Relaxed contiguous-prefix tick-records invariant** (locked from v0.53b onward): `1 ≤ len ≤ TICK_N + 1`, all consecutive starting from 0, tick-0 always present. Required for any window-extension slice that admits early extinction.
- **`B_REACHABILITY_THRESHOLD = 0.25`** (pre-committed pre-data, locked from v0.53b): conservative floor that excludes "uniformly zero or near-zero" reachability.
- **Verdict naming convention `WIDENED_BELOW_REACHABILITY_THRESHOLD_AT_TICK_N`** (locked, safer than the earlier `STRUCTURALLY_UNREACHABLE_AT_TICK_N` form): aligns name with metric; avoids overclaiming at e.g. 20% reachability.
- **Locked phrases reference predecessor verdicts by name** (locked carry-over): v0.53b's priority-4 phrase quotes v0.53's `B_WIDENED_BRIDGE_NOT_FOUND`; v0.53c's priority-4 / priority-6 phrases quote v0.53b's verdict and v0.53's `BRIDGE_PARTIALLY_GENERALIZES` respectively. Embeds the predecessor-stack story directly in the historical record.
- **Implementation note on setup_observer order** (pre-reg vs implementation): v0.53's pre-reg specifies (c) tick-0 → (d) layout-assert; merged implementation does (c) layout-assert → (d) tick-0. Both are read-only pre-dynamics; semantically null. Documented in v0.53 Results and in the script docstring. v0.53b / v0.53c match the merged v0.53 order (no commutative-pair note needed).

## Open questions / blockers

- User has not locked which slice is next. Multiple candidates open: v0.53d cross-layout (substrate variation on widened_gradient), v0.53f investigation of C_food_ladder Label B degradation, Reading-A causal-generalization on tight_gradient + food_ladder, v0.54 joint ablation, fresh-stream calibration.
- No external state blockers. No pending PRs.

## Next concrete step

**`/resume` first, then await user direction on the next slice.** When the user names one, follow the established pre-reg-then-implementation-then-Results pattern: draft pre-reg per the v0.53/v0.53b/v0.53c template, commit pre-reg, ask for thumbs-up on rollup naming + open design questions, then implement (src/ change if needed → reducer → 16 tests → CI gate → reducer run → Results section → commit + push → PR → merge after user confirms CI green). For v0.53d the natural arms are A_null_V0_25 (tier-1 anchor) + B_widened_substrate_variant_X (e.g., relaxed metabolic cost) + possibly C_widened_substrate_variant_Y. For v0.53f, a per-tick-window paired_d trajectory probe on `food_ladder` only. For v0.54 the natural arms are A_null + B_joint_ablation. Cut work onto `claude/review-hedonism-harness-Ryyx0` (the harness-locked branch — DO NOT cut a new branch unless user authorizes).

## Watch-outs

- **Branch is `claude/review-hedonism-harness-Ryyx0`**, not main. Force-push with `--force-with-lease` is the canonical post-squash-merge cleanup pattern (used six times this session combined).
- **No `src/` changes in v0.53 / v0.53b / v0.53c**. Test #8 in each pins the v0.52b-tip src/ SHA-256 hashes. Any future slice that touches `src/` must update both the pre-reg's conservation-framing language AND test #8's pinned hashes (or those tests will halt loud with the locked failure message).
- **Three-window observer + three Label B variants pattern** (introduced in v0.53c) is now the canonical template for any future window-sensitivity slice. Per-tick observer accumulates `pre50_*` / `pre100_*` / `pre200_*` in parallel; Label B is `argmax_lineage(tickN_above_threshold_fraction)` with v0.47 3-tier tiebreak at each window snapshot independently.
- **Extinction is layout × substrate-dependent**: 0% on tight_gradient / food_ladder; 50% on widened_gradient by tick-100; 100% on widened_gradient by tick-200. Future window-extension slices on different layout × substrate cells should expect different extinction trajectories.
- **A_null_V0_25 Label B bridge STRENGTHENS at tick-200** on tight_gradient (events/energy +0.790 → +1.155; distance +1.264 → +2.141). Worth tracking in any Reading-A causal-generalization slice on tight_gradient — tick-200 may be the more discriminative anchor than tick-50 for Label B.
- **C_food_ladder Label B bridge DEGRADES across windows** on food_ladder (events/energy +0.621 → +0.342 → +0.211; below threshold from tick-100 onward). Distance still fires across all windows. Asymmetric Label A vs Label B trajectory worth investigating in v0.53f.
- **Locked phrases fire verbatim**. Each of v0.53 / v0.53b / v0.53c has its locked rollup phrases referencing predecessor verdicts by name. No paraphrase.
- **Per-PR cadence**: pre-reg first commit (record contract), implementation+Results second commit. Each of v0.53 / v0.53b / v0.53c followed this pattern (with 1-2 patch commits between for design-review corrections).
- **CI gate before push**: ruff + format + pytest + smoke. The smoke test (`scripts/core_smoke_test.py`) is unaffected by src/-untouching slices but still verify after every reducer run.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok (228 files already formatted)
uv run pytest                   1744 passed, 7 skipped
uv run python scripts/core_smoke_test.py   ok (default behavior preserved; src/ untouched since v0.52b)
```
