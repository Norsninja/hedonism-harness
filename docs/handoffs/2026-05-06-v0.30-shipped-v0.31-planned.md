# Handoff: v0.30 shipped; v0.31 third-stream tight w*=0.75 calibration planned

**Date:** 2026-05-06
**Branch:** `claude/v0.31-tight-w075-third-stream-calibration`
**Base commit (main tip at branch creation):** `7031dd3` (the branch tip after this handoff is one commit ahead)
**Status:** v0.30 merged to main this session. v0.31 pre-reg + implementation not yet started.

## Where we left off

PR #25 squash-merged (`7031dd3`). v0.30 audited the v0.27 tight w*=0.75 +2-birth interior-optimum claim against fresh seeds 9..16 under the v0.29 methodological rule. Fired **H6 WEAK** (Δ_low=+6, Δ_high=+1, n_favoring=7/8, **n_strict_favoring=0/8**). The v0.27 source (1..8) data fires WEAK under the same partition (Δ_low=+5, Δ_high=+2). Two streams agree directionally on the tight w=0.75 maximum but neither meets the H5 ROBUST ≥5-on-Δ_high bar; per-seed structure shows 6/8 seeds byte-identical across the three weights. v0.31 is the canonical next slice per the v0.30 pre-reg's H6 → "third independent seed stream" decision rule.

## What this session shipped

- v0.30 PR #25 squash-merged: `V0_30_TIGHT_W_ARMS` literal slice; `scripts/v0.30_sweep.py` (24 runs in 8.5s on tight_gradient × seeds 9..16); `scripts/v0.30_audit.py` (new pure-function 4-tier classifier — REVERSAL/ROBUST/WEAK/FAILURE — priority ordered; v0.28 dip-classifier deliberately not invoked); `tests/test_comparison_grid_v0_30.py` (12) and `tests/test_v0_30_audit.py` (12 synthetic-fixture).
- Pre-reg + Results in [[docs/experiments/fear_hunger_v0.30.md]]; per-seed audit report at `runs/fear-hunger-v0.30-tight_gradient/audit.md` (gitignored).
- Test suite 748 → 772 (+24); ruff/format clean; core smoke ok.
- Pre-reg discipline tightened on user notes: H1b clarified as arm-object identity not chamber identity; `n_strict_favoring` added as descriptive non-classifier observable (it caught 0/8 strict in the actual run); WEAK-verdict wording sharpened (Δ_low=+6 non-trivial; Δ_high=+1 sample-noise-scale).

## Files touched (wikilinks for fast load)

- [[docs/experiments/fear_hunger_v0.30.md]] — pre-reg + Results; H6 WEAK fires; documents the v0.27 vs v0.30 cross-stream agreement under the same 4-tier partition.
- [[src/hedonism_harness/experiments/comparison_grid.py]] — `V0_30_TIGHT_W_ARMS = tuple(arm for arm in V0_27_ARMS if arm.label in {...})` literal-subset construction (mirrors v0.29).
- [[scripts/v0.30_sweep.py]] — sweep (mirrors v0.29 sweep verbatim except chamber/batch_id/arms).
- [[scripts/v0.30_audit.py]] — audit driver. **`evaluate_audit(b50_at, seeds) -> AuditOutcome`** is the canonical reusable peak-audit classifier. Imports v0.28 module via importlib (hyphen-name pattern from v0.29).
- [[tests/test_comparison_grid_v0_30.py]], [[tests/test_v0_30_audit.py]] — +12 + 12 tests.

## Decisions locked in

- **v0.27 tight w*=0.75 +2-birth claim demoted, not falsified.** Two streams agree directionally; neither meets H5 ROBUST. Phrase in forward references as: "small-margin weight-(in)sensitive at hazard=8; directionally reproducible across two independent 8-seed streams; per-seed strict preference absent in both."
- **`evaluate_audit(b50_at, seeds)` is the canonical 4-tier peak-audit classifier.** The v0.28 H5/H6/H7 dip-classifier is for valleys; the v0.30 4-tier is for peaks. v0.31 reuses v0.30's classifier directly.
- **Methodological rule (v0.29) holds.** A second WEAK verdict on a fresh stream does NOT promote to mechanism; it calibrates the directional reproducibility of the small-margin signal.
- **Per-version branch cadence preserved.** Each version PR cuts a new `claude/v0.NN-...` branch; user manually prunes after merge.
- **`n_strict_favoring` is the load-bearing descriptive observable for ties-vs-strict ambiguity.** Pre-commit it on every peak audit going forward.

## Open questions / blockers

- **None blocking.** v0.31 pre-reg + sweep + audit can start on this branch immediately.
- **Remote branches** `claude/v0.30-...` was pruned by the user. Earlier `claude/v0.26-...` / `claude/v0.27-...` / `claude/v0.28-...` / `claude/v0.29-...` may still be present remotely; sandbox proxy cannot delete them.

## Next concrete step

**Write the v0.31 pre-reg first**, then build the sweep. **Single target:** tight_gradient × hazard=8 × influx=1.0 × w ∈ {0.5, 0.75, 1.0} × seeds **17..24** (3 arms × 8 seeds = 24 runs; ~8s; mirrors v0.30 sweep).

The v0.31 pre-reg should:
- **Frame the question narrowly per the user's v0.30 review note:** "Does the aggregate weak direction compound across three independent streams, or does it collapse?" — *NOT* "is the tight w*=0.75 optimum a mechanism." Mechanism promotion is OUT OF SCOPE.
- **Reuse the v0.30 4-tier classifier directly** via `evaluate_audit(b50_at, seeds)` (single-stream verdict on seeds 17..24 only).
- **Pre-commit a 24-seed pooled rule** for the 3-stream calibration question. The user explicitly flagged: "Do not use a third weak result to promote mechanism unless the pre-reg defines a pooled 24-seed rule in advance." Suggested rule: pool b>50 across streams 1..8, 9..16, 17..24 (24 seeds total), apply the same 4-tier classifier to the pooled aggregates with proportionally-scaled thresholds (e.g., `Δ_threshold_pooled = 5 × 24/8 = 15` — to be debated in the pre-reg). Pooling preserves the methodological rule's discipline; arbitrary thresholds do not.
- **Define a `V0_31_TIGHT_W_ARMS` literal slice of V0_27_ARMS** at the same 3 labels. By symmetry it'll be element-wise identical to `V0_29_ARMS` and `V0_30_TIGHT_W_ARMS`. Prefer a fresh literal slice over aliasing for substrate-identity-by-construction discipline.
- **Carry forward the per-seed weight-insensitivity observation as a primary finding candidate** — even if all three streams fire WEAK, the pooled per-seed pattern (likely ≥ 18/24 seeds byte-identical across weights) may be the more important result.

Driver scripts: `scripts/v0.31_sweep.py` (single sweep on seeds 17..24) + `scripts/v0.31_audit.py` (single-stream verdict + cross-stream comparison table + pooled 24-seed verdict if pre-reg adopts the pooled rule).

**Do not** add the v0.25 hazard-axis audit to this PR (originally listed as the next priority in the v0.30 handoff, but the H6 WEAK verdict promotes the third-stream calibration ahead of it). v0.25 hazard-axis audit is now v0.32+.
**Do not** introduce a finer weight grid — H5 ROBUST is the gate for that.
**Do not** sweep seeds 25..32. One additional stream per PR.

## Watch-outs

- **Pre-reg the pooled 24-seed rule BEFORE seeing the data.** The whole point of v0.31 is methodological discipline; defining the pooled threshold post-hoc would defeat the purpose. The user's note is explicit on this.
- **Determinism:** the v0.30 audit classifier (`evaluate_audit`) is pure-function and unit-tested; if v0.31 refactors it (e.g., to take 3 streams), keep the existing 12 synthetic-fixture tests passing as anchor.
- **The descriptive `n_strict_favoring` observable proved load-bearing in v0.30** (caught 0/8 strict despite 7/8 favoring). Carry it forward; it'll likely repeat the pattern on stream 3.
- **Substrate-identity-by-construction pattern.** `V0_31_TIGHT_W_ARMS` should be a literal `tuple(arm for arm in V0_27_ARMS if arm.label in {...})` slice. The arm-object-identity test against V0_29_ARMS/V0_30_TIGHT_W_ARMS is by-construction.
- **Test suite at 772.** Pure-additive v0.31 should bring it to ~790 depending on pooled-classifier test coverage.
- **The v0.27 doc still references "tight w*=0.75 interior optimum" in its Substantive Findings section.** Forward-reference cleanup is OUT OF SCOPE for v0.31 (would mutate a prior pre-reg's results doc); instead, v0.31's Results should explicitly link the v0.27 / v0.28 / v0.29 / v0.30 / v0.31 chain on this question.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   772 passed
uv run python scripts/core_smoke_test.py  ok
```
