# Handoff: v0.35 + v0.36 shipped; CLAUDE.md landed; v0.37 descendant-trait drift planned

**Date:** 2026-05-06
**Branch:** `main` (no active feature branch yet — cut v0.37 branch from `9c86437` when starting)
**Tip commit:** `9c86437`
**Status:** Three slices merged this session (v0.35 #30, v0.36 #31, CLAUDE.md #32). Founder-trait amplification with hazard is **ruled out** as the mechanism for v0.35's early-leader continuity. Next slice is locked-by-user-intent but pre-reg not yet drafted; **do NOT autostart implementation** — trait inventory + locked comparison set + user sign-off, then pre-reg.

## Where we left off

v0.36 fired **H6 TRAIT-LINKED-FLAT**: `sensor_radius` is a robust hazard-INDEPENDENT founder-trait predictor of post-50 winning (winner mean ≈5.2 / non-winner ≈3.2; |d| 1.18–1.35 across all four hazards; spread +0.082 < locked 0.2 strengthening threshold). `reproduction_drive` and `metabolic_rate` both fail magnitude bar at h=12. The v0.35 early-leader-continuity story is therefore **not** explained by hazard-amplified founder-trait selection on this corpus. User chose to pause for session boundary rather than push through to v0.37 immediately. CLAUDE.md ([[CLAUDE.md]]) was authored mid-session and is now on main; future sessions auto-load it.

## What this session shipped

- v0.35 PR #30 squash-merged (`afd738b`): founder-survival-timing replay; **H6 EXPANSION-SUPPORTED ≡ EARLY-LEADER CONTINUITY**; locked dual phrasing (early-leader continuity ≠ measured post-50 expansion rate).
- v0.36 PR #31 squash-merged (`eeaa3ba`): founder-trait heritability replay; **H6 TRAIT-LINKED-FLAT**; sensor_radius firing aligned-flat; reproduction_drive shows opposite-sign at h=0 (descriptive only); metabolic_rate aligned but attenuates.
- [[CLAUDE.md]] PR #32 squash-merged (`9c86437`): identity (Chronus from TD), handoff-is-for-Chronus-only definition, project conventions (pre-reg cadence, post-hoc reducer pattern, re-anchor discipline, locked phrases, trait-fishing protection, halt-loud).
- Test suite **883 → 924 → 972** (+41 v0.35 + 48 v0.36). Ruff / format / smoke green throughout.

## Files touched (wikilinks for fast load)

- [[CLAUDE.md]] — project working agreement; identity + handoff definition + conventions. Auto-loaded into every future session.
- [[docs/experiments/fear_hunger_v0.35.md]] — v0.35 pre-reg + Results; H6 EXPANSION-SUPPORTED; locked dual phrasing.
- [[docs/experiments/fear_hunger_v0.36.md]] — v0.36 pre-reg + Results; H6 TRAIT-LINKED-FLAT; sensor_radius firing.
- [[scripts/lineage_survival_replay.py]] — v0.35 reducer (~590 LOC); imports v0.34 helpers via importlib.
- [[scripts/trait_replay.py]] — v0.36 reducer (~600 LOC); imports v0.34 helpers via importlib.
- [[tests/test_lineage_survival_replay.py]] — 41 tests.
- [[tests/test_trait_replay.py]] — 48 tests.

## Decisions locked in

- **v0.35 verdict: H6 EXPANSION-SUPPORTED ≡ EARLY-LEADER CONTINUITY.** Locked dual phrasing throughout; observable measures `winner_already_dominant_at_tick_50_rate`, NOT post-50 expansion rate.
- **v0.36 verdict: H6 TRAIT-LINKED-FLAT.** Hazard-amplified founder-trait selection ruled out as v0.35 mechanism on this corpus.
- **v0.36 locked constants:** `PRIMARY_TRAITS = (reproduction_drive, metabolic_rate, sensor_radius)`; `EXPECTED_SIGN = {+1, -1, +1}`; `ABS_D_THRESHOLD = 0.4`; `STRENGTHENING_SPREAD_THRESHOLD = 0.2`. Opposite-sign-strengthening cannot fire H5 (locked guardrail).
- **Conservation:** zero `src/` edits across v0.34/v0.35/v0.36; zero edits to prior `scripts/v0.NN_*.py`; [[scripts/lineage_replay.py]] is in production from v0.35 onwards and must remain byte-identical.
- **Re-anchor stack now triple-deep:** v0.36 anchors against v0.34 `run_summary.csv:top_lineage_id` AND v0.35 `pre_post_dominance.csv:eventual_top_lineage_id`. v0.37+ should continue this discipline.
- **Trait-fishing protection (per [[CLAUDE.md]]):** small pre-committed comparison sets with mechanistic priors; no Bonferroni; no p-values; effect-size discipline only.

## Open questions / blockers

- **v0.37 question (locked-by-user-intent):** does descendant-trait drift accumulate trait perturbations under stronger selection at high hazard? This is the most direct follow-up to v0.36's "founder traits don't strengthen with hazard" finding. Pre-reg not yet drafted.
- **Trait inventory for descendants is NOT yet done.** Founder-only inventory is in [[docs/experiments/fear_hunger_v0.36.md]]; descendants drift from parents in 1–2 trait coordinates per generation per the v0.36 inspection notes. Need to inventory parent-child Δ distributions BEFORE pre-reg locks the comparison set, mirroring the v0.36 trait-inventory-first discipline.

## Next concrete step

**Cut `claude/v0.37-descendant-trait-drift` branch from `9c86437`.** Then trait-inventory phase (no per-trait analysis yet): for one or two representative runs from the 96-run corpus, compute parent → child trait deltas and characterise the perturbation distribution (which traits drift; magnitudes; per-generation rate). Bring inventory back to user. **Wait for user sign-off on the locked descendant comparison set before drafting pre-reg.** Candidate framings to propose: (a) winner-lineage descendant-drift magnitude vs non-winner-lineage descendant-drift magnitude per primary trait, per hazard; (b) per-generation drift direction (toward / away from founder mean) by survival outcome; (c) cumulative drift from founder by post-50 b50 contribution. Pre-commit threshold + monotone-with-hazard rule before any per-lineage inspection. Output reducer at `scripts/descendant_drift_replay.py`; reuse v0.34 helpers + read v0.36 `founder_traits.csv` + v0.35 `pre_post_dominance.csv` as anchors. Estimated ~300 LOC reducer + ~250 LOC tests + ~500 LOC pre-reg.

## Watch-outs

- **CLAUDE.md is now on main.** Future sessions auto-load it — `/resume` should read it before the latest handoff. Don't re-derive its protocols from session transcripts.
- **Pre-reg before sweep / reducer.** v0.37 must not skip the inventory-then-lock-then-implement cadence. v0.36 was the canonical fishing-prevention example; reuse that discipline.
- **Reuse v0.34/v0.35/v0.36 helpers additively.** [[scripts/lineage_replay.py]], [[scripts/lineage_survival_replay.py]], [[scripts/trait_replay.py]] are in production. Import via `importlib.util`. Do NOT modify.
- **Re-anchor against v0.36's `founder_traits.csv`** (per (source_version, arm_label, seed, lineage_id) + per-trait values byte-identical). Drift halts via `LineageReplayError`.
- **Descendant lineage_id semantics:** v0.34's `assign_founder_lineages` walks parent chains to assign descendants the founder's lineage_id. `trait_fingerprints.csv` already does this correctly (verified during v0.36 inventory). Use v0.34's authoritative assignment, not the on-disk `lineage_id` column.
- **Don't bundle HedonismPolicy or Mesa.** Both remain deferred indefinitely per CLAUDE.md.
- **`runs/lineage-v0.37/` is gitignored** under `runs/`. Outputs are local-regenerable.
- **Cautious framing remains locked:** "consistent with X on the v0.34 corpus," not "X causes Y." Even a clean v0.37 H5 would be correlational; promotion to mechanism requires fresh-stream calibration analogous to v0.30..v0.33.

## CI gate at handoff time

```
uv run ruff check .             ok
uv run ruff format --check .    ok
uv run pytest                   972 passed
uv run python scripts/core_smoke_test.py  ok
uv run python scripts/trait_replay.py     H6 TRAIT-LINKED-FLAT (re-runnable)
```
